# Page State - 2026-05-25 00:00 +08:00

## Routes

- `/`: `BasicLayout` + `AgentWorkspace`
- `/design-system`: `BasicLayout` + `DesignSystem`

## Workspace Tabs

- `home`: unchanged.
- `bankapp-demo`: unchanged.
- `whatsapp-demo`: unchanged.
- `live-chat`: fixed tab after Sign In; label is `Live Chat` when no active session and `Live Chat (mm:ss)` when active sessions exist.
- `inbound`: reused for PSTN and BankApp Voice; label is `PSTN (mm:ss)` for PSTN and `Voice Call (mm:ss)` for BankApp Voice while timing is active.
- `video-call`: label is `Video Call (mm:ss)` while timing is active.

## Live Chat

- Only active sessions are visible; Webchat remains hidden.
- BankApp Live Chat adds `live-chat-002` and backdates runtime by mock `accessDuration` (`02:11`).
- WhatsApp Demo adds `live-chat-001` and backdates runtime by mock `accessDuration` (`00:48`).
- Customer list expanded state shows `lastMessageTime · duration`, SLA duration color, and a left accent for warning/breach.
- Customer list collapsed state shows SLA marker on the channel icon.
- Conversation header timer shares the same runtime elapsed value as the list and tab.

## Cleanup Paths

- Hang Up clears voice/video interaction timing.
- Closing Inbound/Video Call tab clears the related tab timing.
- End Service clears that Live Chat session timing.
- Sign Out / AUX clears call timing and active Live Chat session timing.

## Validation

- `npm run lint`: passed.
- `npm run build`: passed with existing chunk size warning.
- Browser smoke `/` and `/design-system`: passed.
