# Page State - 2026-06-15 15:28 +08:00

## `/call-management/verification-rule-v2`

- Outer list remains standard admin CRUD layout.
- Edit/Add modal now has scenario cards under `Question Configuration`.
- Scenario cards show scenario name, correct required count, and block count.
- `+ Scenario` opens a modal with scenario name and create mode.
- Fixed block add buttons are `Mandatory`, `Dynamic`, `Static`, `Alternative`, plus `Custom Block`.
- Fixed block titles are not editable.
- Custom block names are editable.
- `Alternative` has no Correct input and keeps tooltip guidance.
- `Preview` opens a Customer Verification preview with current draft rule.

## Customer Verification Popup

- Channel remains read-only.
- Skill Queue and Customer Segment remain demo controls.
- Scenario selector is hidden for single-scenario rules.
- Scenario selector appears for multi-scenario rules.
- Organization Segment selector is removed from the popup.

## Data

- `VerificationV2QuestionBlock.blockType` is optional for backward compatibility.
- Runtime normalization infers old fixed blocks by exact names and marks other blocks as `custom`.
- `v2-kbb-organization` now contains `O1-O3` and `O4-O5` scenarios.
