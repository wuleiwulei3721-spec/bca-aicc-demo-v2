# Current TODO - 2026-06-17 18:57 +08:00

## P0

- None for the current query filter and mock data adjustment.

## P1

- Manually review `Priority List Management`:
  - Query area contains `Channel`, `Identifier`, and `Match Rule`.
  - `Match Rule` filter defaults to all.
  - Filtering by `Partial Match` shows the `@ojk.co.id` / `@bi.go.id` sample records.
  - Filtering by `Exact Match` hides those partial-match domain samples and keeps exact-match samples.
  - Batch Add still defaults to `Exact Match` and can save `Partial Match` when selected.

## P2

- Confirm backend matching implementation later:
  - `Exact Match` uses equality.
  - `Partial Match` uses contains.
- Consider adding demo narration that `@domain` examples are maintained as partial-match samples.

## Notes

- Blacklist Management remains unchanged.
- No automatic email domain matching logic was restored in the page save flow.
