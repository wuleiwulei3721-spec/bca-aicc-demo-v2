# Current TODO - 2026-06-12 17:03 +08:00

## P0

- Manually verify `/call-management/verification-rule-v2` in the demo browser because automated screenshot verification could not run.
- Confirm `Question Bank` and `Add` sit in the filter toolbar right-side action area.
- Confirm the table header, filter, modal field, and validation message all use `Skill Queue`.
- Confirm normal desktop width does not show an unnecessary horizontal scrollbar and the `Actions` column remains fixed when the viewport is narrow.

## P1

- For future custom management pages, apply the existing `RoutingConfigCrudPage` toolbar, action, table, and CRUD checklist before adding business-specific UI.
- Consider adding lightweight UI smoke coverage for management pages to catch button placement, CRUD completeness, and label drift.

## P2

- If visual regression checks become required, add a project-approved browser test dependency instead of relying on ad hoc runtime Playwright availability.
