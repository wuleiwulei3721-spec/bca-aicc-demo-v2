# Current TODO - 2026-05-27 22:56 +08:00

## P0

- 在用户本地浏览器人工复查 Transfer 弹框：
  - `Transfer Agent` 有姓名/工号、`Skill Queue`、`Status` 三类筛选。
  - 列表显示 `Skill Name`、`Status`，状态 tag 不撑高行高。
  - `Transfer Number` 只显示一行号码输入框 + `Transfer` + `Conference`。
- 在用户本地浏览器人工复查 Outbound Call 弹框：
  - `Call Agent` 有 `Skill Queue`、`Status` 筛选。
  - 列表新增技能名称和状态，`Call` 行内按钮保持紧凑。
- 发布前再次执行 `git diff --name-only main..HEAD`，确认没有 `livechat2` / `LiveChat2` 文件。

## P1

- 如用户希望技能队列示例改成中文/印尼语，需要统一 Transfer / Outbound / mock 的展示语言口径。
- 如评审认为 860px 弹框内列仍过密，可优先减少 Department 列宽或隐藏 Department，而不是扩大弹框影响其它页面。

## P2

- 后续如真实接入坐席目录，可将 `TransferAgentStatus` 与技能队列改为接口字段，并保留当前 mock 作为 demo fallback。
