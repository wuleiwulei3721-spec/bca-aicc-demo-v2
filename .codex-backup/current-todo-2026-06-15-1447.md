# Current TODO - 2026-06-15 14:47 +08:00

## P0

- Manually verify `/call-management/priority-list` in browser.
- Verify Channel search is multi-select and empty means All Channels.
- Verify Add / Batch Add Channel is multi-select and does not include `All Channels`.
- Verify all customer-visible `Priority Number` labels are now `Identifier`.
- Verify Identifier tooltip content and icon placement.
- Verify Batch Add with `0811111111;@ojk.co.id;user@example.com` creates three records.
- Verify `@ojk.co.id` shows `Email Domain Match`.
- Verify phone number and email address values show `Exact Match`.
- Verify selected multiple channels remain displayed as one row with channel tags.
- Verify checkbox Delete still removes selected Priority List records.

## P1

- Confirm with customer whether automatic email-domain detection is sufficient or whether a future explicit Identifier Type is needed.
- Confirm whether backend execution should split one UI record into channel-specific matching rows.

## P2

- Add automated interaction tests for multi-select channel filtering and Match Rule derivation if a browser test harness is introduced.
