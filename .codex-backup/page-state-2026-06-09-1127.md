# Page State - 2026-06-09 11:27 +08:00

## Header

- Right side actions include notification, internal chat, profile/status area, and a separate red `Log Out` power button.
- The red `Log Out` button clears auth session, resets media state to `Unsigned`, and navigates to `/login`.

## Agent Profile Dropdown

- `Unsigned`: dropdown shows a `Sign In` group with text-only `Voice only`, `Digital only`, and `Voice + Digital`.
- Signed in: dropdown shows `Signed in: {mode}`, an `AUX` group with enabled busy reasons, and media `Sign Out`.
- Dropdown no longer contains system `Log Out`.
- Profile second line shows `PBK BSB | {mode}` after media sign-in.

## Skill Enforcement

- `Digital only` blocks PSTN/voice/video handoff with the existing Header warning.
- `Voice only` blocks WhatsApp/BankApp live chat handoff with visible `bankapp-process__handoff-warning`.
- `Voice + Digital` allows PSTN and existing demo handoff paths.

## Validation State

- Completed: `npm run lint`.
- Completed: `npm run build`.
- Completed: local Chrome CDP smoke for login redirect/errors/success, grouped sign-in menu, media sign-out, system logout, `/design-system`, Digital-only PSTN block, Voice + Digital PSTN allow, and Voice-only WhatsApp live chat block.
