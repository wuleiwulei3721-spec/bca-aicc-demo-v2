# Page State - 2026-05-23 13:30 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## BankApp Demo

- Entry: `Customer Simulator > BankApp`.
- Workspace tab: `BankApp Demo`, closable.
- Layout: left `Customer BankApp` phone simulator and right `AICC Process` rail.
- Controls: `Customer Type`, `Next Step`, `Reset` inside AICC Process.
- Language selector is removed; current language is fixed in component state.
- Agent Desktop Outcome panel is removed; agent result appears through real workspace tabs.

## Screenshot State

- Channel image:
  - `public/screenshots/bankapp/channel-selection-sanitized.png`
  - Referenced by `bankAppScreenshotSources.channel`.
  - Shows `BANK 1` and only clear service entries for `Voice Call`, `Video Call`, `Live Chat`.
- Guest phone number image:
  - `public/screenshots/bankapp/voice-phone-number-sanitized.png`
  - Referenced by `bankAppScreenshotSources.voicePhoneNumber`.
- Livechat personal information image:
  - `public/screenshots/bankapp/text-login-sanitized.png`
  - Referenced by `bankAppScreenshotSources.textLogin`.
- Original customer screenshots remain in `public/screenshots/bankapp/` and are not currently referenced by BankApp Demo for these three entry pages.

## Interaction State

- Registered Voice/Video skips `Input Phone Number`.
- Guest Voice/Video shows the sanitized phone number image before business selection.
- Livechat shows the sanitized customer information image before business selection.
- `Select Business` and later screens are generated React UI.
- Voice flow triggers `requestBankAppVoiceCall()` and opens PSTN / Voice Call with `BankApp Voice`.
- Video flow triggers `requestBankAppVideoCall()` and opens Video Call with `BankApp Video`.
- Livechat flow triggers `requestLiveChatWorkspace('live-chat-002')` and focuses BankApp customer Sari Amelia.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with existing chunk size warning.
- Browser `/`: passed for sanitized channel image path, three hotspot flows, Guest Voice phone image, and Livechat personal information image.
- Browser `/design-system`: passed.

## Rollback

- Restore `src/mock/bankapp.ts` image paths to:
  - `channel-selection.png`
  - `voice-phone-number.png`
  - `text-login.png`
- Optional cleanup: remove the three `*-sanitized.png` files if no longer needed.
