# Current TODO - 2026-06-04 21:32 +08:00

## P0

- Manually re-check `Routing Config > Channels > Account Management` at the target demo resolution:
  - Instagram shows two account cards.
  - Account cards do not overflow horizontally.
  - Add/Edit Account secondary modal fits inside the dialog.
  - Edit/Delete account actions remain visible and clickable.
- Manually re-check `Channels` list:
  - No Add/Delete Channel.
  - Columns show Channel ID, Channel Name, Channel Type, Media Type, Account Count, Status, Actions.
  - Actions order is Edit, Accounts, Business Config.
- Manually re-check `Channels > Edit`:
  - Basic Information and Access Parameters are separated.
  - Media Type and Status are editable.
  - Channel ID, Channel Name, and Channel Type remain read-only.
- Manually re-check `Channels > Business Config`:
  - No redundant Customer Service Configuration wrapper.
  - Text media shows full text service behavior without queue settings.
  - Voice/Video show access configuration only.
  - Message variable insertion works at the cursor position.
- Manually re-check old routes:
  - `/routing-config/access-accounts` redirects to `/routing-config/channels`.
  - `/routing-config/media-service-rule-plans` redirects to `/routing-config/channels`.

## P1

- Continue refining Channels business configuration wording and grouping after user review.
- Continue checking `Skill Queues` queue settings after moving queue configuration out of media service rules.
- Revisit whether account delete protection should be stricter once backend references are modeled.

## P2

- Consider adding lightweight UI tests for Routing Config modal flows once the demo structure stabilizes.
- Consider future code splitting for the large Vite bundle warning.
