# Current TODO - 2026-06-11 17:43 +08:00

## P0

- Completed: `npm.cmd run lint`.
- Completed: customer-mode build with `VITE_ENABLE_ADMIN_MENUS=false`.
- Completed: customer-mode bundle check confirmed `enableRoutingConfigMenus: false`, `Call Management` label present, and `Text Channel Settings` label absent.
- Completed: pushed `main` so Vercel Production published the corrected customer menu policy.
- Completed: Production homepage references the new `/assets/index-hJHC-o_T.js` asset.
- Completed: production JS confirms `{enableRoutingConfigMenus:!1}`, `Call Management` label present, and `Text Channel Settings` label absent.

## P1

- Browser smoke check customer mode: left menu shows `Call Management`, does not show `Routing Config`.
- Browser smoke check customer mode: `/call-management/verification-rules`, `/call-management/global-control-configuration`, and `/call-management/busy-reasons` render after login.
- Browser smoke check customer mode: `/routing-config/channels` redirects to `/`.

## P2

- Consider renaming `VITE_ENABLE_ADMIN_MENUS` to a routing-specific flag in a future cleanup, after updating Vercel env configuration.
- Add automated route visibility checks for customer and engineering modes.
