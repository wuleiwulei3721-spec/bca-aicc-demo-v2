# Current TODO - 2026-05-27 23:39 +08:00

## P0

- 人工复查 Internal Chat 弹框底部是否符合截图：无边框无背景输入区，右下角蓝色图标 `Send` 按钮。
- 确认左下角不显示 Emoji、Attach、Upload image 等工具图标。
- 确认 Send 按钮图标、文字、尺寸和圆角接近截图。
- 发布前再次执行 `git diff --name-only main..HEAD`，确认没有 `livechat2` / `LiveChat2` 文件。

## P1

- 如果按钮仍偏大或偏小，可只调整 `.aicc-internal-chat__composer-toolbar .aicc-button` 的 width/height/padding。

## P2

- 后续可将 composer 的极简模式沉淀为共享样式，供 Internal Chat 和 Live Chat 复用。
