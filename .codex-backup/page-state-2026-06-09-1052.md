# Page State - 2026-06-09 10:52 +08:00

## Routes

- `/` loads the authenticated agent workspace after demo login/session.
- `/call-management/busy-reasons` opens Busy Reason Management.
- `/routing-config/*` still redirects to `/`.

## AUX Dropdown

- Service unsigned: avatar menu shows service sign-in options and `Log Out`.
- Service signed in: avatar menu shows current service mode, `AUX` group title, enabled busy reasons, `Sign Out`, and `Log Out`.
- `AUX` is a group title with presentation semantics, not a selectable busy reason.
- Busy reason items show plain text only and no repeated icons.
- Clicking a busy reason immediately sets the status label to `AUX - {reasonName}`.

## Validation State

- Completed: `npm run lint`.
- Completed: `npm run build`.
- Completed: local Chrome CDP smoke check for signed-out menu, `AUX` group title, zero icons on reason items, group-title no-op click, and `Break` / `Keperluan Pribadi` status changes.
