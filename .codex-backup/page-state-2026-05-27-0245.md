# Page State - 2026-05-27 02:45 +08:00

## 路由与菜单

- `/` -> `BasicLayout` -> `AgentWorkspace`。
- `/design-system` -> `BasicLayout` -> `DesignSystem`。
- `Channel Simulation` 当前包含：`PSTN`、`BankApp`、`WhatsApp`、`livechat2`。
- `livechat2` 菜单位于 `WhatsApp` 下方，点击后打开独立 `livechat2` workspace tab。

## Workspace Tabs

- `Home`：保持原样。
- `Live Chat`：旧固定文字聊天工作台，保持原样。
- `livechat2`：新增并行新版文字弹屏 tab，不复用旧 `live-chat` key。
- PSTN / Voice / Video call tabs：保持原逻辑。

## livechat2 页面状态

- 左侧 lead panel：当前服务用户 + 历史用户。
- 当前服务用户：4 个 mock 客户，渠道覆盖 WhatsApp、BankApp、Webchat。
- 历史用户：1 个 mock 历史客户，关闭结束会话后也会进入历史列表。
- Center CRM：`Conversation` tab 使用 `LiveChat2ConversationWorkspace`，`CRM` 和动态业务 tab 保持复用。
- Left Column：继续复用 Customer Information、Customer Journey、Ticketing History、Next Best Action、Quick Action。
- Right Column：继续复用 Assistant / Connection。

## 已验证交互

- 菜单打开：`Channel Simulation > livechat2`。
- 客户切换：Dimas / Sari 等客户切换后三栏联动。
- 快捷回复：输入 `/aa` 后显示浮层并可带入发送。
- 发送消息：客户列表最后消息同步更新，未回复计时清零。
- 消息记录：Conversation 右侧 Message Record 可打开。
- Transfer：Conversation 版 Transfer 弹框可打开。
- 旧功能：旧 `Live Chat` 空态可打开，`WhatsApp Demo` 可打开，`/design-system` 正常。

## 已知限制

- `livechat2` 不接真实后端；所有消息、历史、文件、图片、引用、撤回、排序和结束状态均为前端演示。
- 当前没有真实声音提醒、后台配置、文件上传下载、截图插件、拼写检查或敏感词拦截。
- 需要用户在目标演示屏幕上继续人工调试视觉细节。
