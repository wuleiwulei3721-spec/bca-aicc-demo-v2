# Page State - 2026-05-28 18:08 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## livechat2 History List

- History 展开态客户行第一行右侧显示 `DisconnectOutlined + endTimeDisplay`。
- `endTimeDisplay` 常显具体时间：
  - 当天：`HH:mm:ss`。
  - 非当天：`MM-DD HH:mm:ss`。
- `title` / `aria-label` 保留完整 `Ended ${endTimeDisplay}`。
- 当前列表不显示结束图标或结束时间。
- 面板收起态不显示结束图标或结束时间。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，保留既有 chunk size warning。
- Browser `/`：可加载 Home tab。
- Browser `/design-system`：可加载 Design System。
- livechat2 菜单点击与 History 视觉仍需人工复查。
