# Page State - 2026-05-22 01:02 +08:00

## Routes

- `/`: main workspace via `BasicLayout` and `AgentWorkspace`.
- `/design-system`: design system page via `BasicLayout`.
- `*`: redirects to `/`.

## Live Chat Page

- Component: `src/pages/inbound/LiveChatPage.tsx`.
- Customer list component: `src/pages/inbound/components/LiveChatCustomerList.tsx`.
- Shared workspace container: `src/pages/inbound/InteractionWorkspace.tsx`.
- Sign In still inserts the fixed, non-closable `Live Chat` tab next to Home.
- Live Chat customer list now starts collapsed.
- Header filters are icon-only: ALL, WhatsApp, BankApp, Webchat; tooltips expose the labels.
- Filtering updates the list data and, when needed, moves the active session to the first matching session.
- Customer rows use channel icons and unread badges instead of customer avatars.
- Row metadata is reduced to customer name, last message time, and last message.
- Row-level channel tag and High priority tag are removed.
- BankApp visible label is used for internal `Haloapps` chat sessions.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with existing Vite chunk size warning.
- Browser `/`: passed for Sign In, Live Chat default collapsed state, expand behavior, ALL/WhatsApp/BankApp/Webchat filters, BankApp filtering, and Customer Information sync.
- Browser `/design-system`: passed.
