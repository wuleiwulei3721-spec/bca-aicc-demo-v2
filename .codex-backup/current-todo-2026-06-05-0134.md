# Current TODO - 2026-06-05 01:34 +08:00

## P0

- Manually review `Routing Config > Skill Queues` Add/Edit/View modal:
  - `Basic Information` appears first.
  - `Queue Configuration` appears below it.
  - Queue fields are grouped as non-working message, max queue + waiting message, timeout + timeout message.
  - Add/Edit show `Insert Variable` beside message labels.
  - View does not show `Insert Variable`.
  - Variables insert at the current cursor position.
  - Save updates the list and preserves queue fields.

## P1

- Confirm whether `Queue Timeout Message` should support more variables, such as `{queuePosition}`, `{skillQueueName}`, or `{channelName}`.
- Decide whether Skill Queues should eventually support media-specific queue prompts beyond the current simple queue fields.
- Regression-check default CRUD pages because `RoutingConfigCrudPage` gained an optional custom renderer, although default behavior is unchanged.

## P2

- Consider extracting shared message variable insertion into a small helper if a third page needs the same behavior.
- Consider aligning all queue-related default mock messages to the final customer language once the demo language decision is settled.
