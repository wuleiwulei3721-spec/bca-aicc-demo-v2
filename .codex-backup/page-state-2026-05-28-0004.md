# Page State - 2026-05-28 00:04 +08:00

## Transfer Modal

- `Transfer Agent` 页签保留姓名/工号、`Skill Queue`、`Status` 筛选。
- 坐席列表保留 `Skill Name`、`Status`，移除 `Department` 以释放宽度。
- `Transfer Number` 页签为单行布局：号码输入框 + `Transfer` + `Conference`。
- `Transfer Number` 号码输入框和两个按钮已统一为 30px 控件高度。

## Outbound Call Modal

- `Call Agent` 页签保留姓名/工号、`Skill Queue`、`Status` 筛选。
- 坐席列表保留 `Skill Name`、`Status`，移除 `Department` 以释放宽度。
- `Call Number` 页签为单行布局：`Phone Number` label + 带 phone prefix 的号码输入框 + `Call`。
- `Call Number` 输入框、prefix icon 和 `Call` 按钮已统一为 30px 控件高度。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过。
- 仍建议在当前浏览器中人工打开 Transfer / Outbound 深层弹框，按目标演示分辨率复查视觉对齐。
