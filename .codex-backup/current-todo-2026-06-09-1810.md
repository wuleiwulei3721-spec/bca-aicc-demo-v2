# Current TODO - 2026-06-09 18:10 +08:00

## P0

- Manually review `/routing-config/channels` in the normal browser after this change, especially the Channels Edit modal and Business Config modal for Instagram, Haloapp, Webchat, and WhatsApp.
- Keep `codex/admin-config-latest` local/internal. Do not merge or publish it to customer Production.

## P1

- Decide whether AppStore and PlayStore should be treated as social-like channels for access concurrency/scan interval. Current implementation excludes them because their Channel Type category is `app-store`, not `social`.
- If Route Elements or Channel Type Management becomes needed again, restore menu and route explicitly instead of relying on hidden component source.

## P2

- Consider adding automated UI smoke coverage for admin menu visibility and key modal field presence.
- If Vite chunk warning becomes a deployment concern, revisit code splitting.
