# Page State - 2026-05-29 15:39 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## livechat2 Quick Replies

- `Quick Replies` remains a fixed livechat2-only right tab after `Connection`.
- The `My Phrases` title add action is a compact 22px hover/focus button aligned with lower action buttons.
- Section and group collapse arrows use system subdued color by default and theme primary on hover/focus.
- Existing Quick Replies CRUD, wrapping, group collapse, and `/` popup sync behavior remain unchanged.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with the existing Vite/Rolldown chunk size warning.
- `git diff --check`: passed with CRLF warnings only.
- Actual livechat2 Quick Replies visual flow: still needs manual review.
