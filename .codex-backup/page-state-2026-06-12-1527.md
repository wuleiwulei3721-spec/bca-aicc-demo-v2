# Page State - 2026-06-12 15:27 +08:00

## Verification Rule V2

- Route: `/call-management/verification-rule-v2`.
- `Question Configuration` remains add-on group based.
- `Special Rules` now mirrors that pattern:
  - Add row: `Select special rule type` + `Add Rule`.
  - Empty state: `No special rule configured.`
  - Visible cards only for enabled special rules.
  - Delete icon disables the rule and preserves its internal values.
- Available special rules:
  - `Case Type Layering`
  - `Branch Combined Verification`
  - `Organization Segment Override`
- `Strict Failure Handling` no longer appears in V2 UI or source data model.
- `Failure Action` remains in the base form and controls whether failed verification blocks continuation.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with existing chunk-size warning.
- `git diff --check`: passed with existing CRLF conversion warnings only.
- HTTP smoke: `/`, `/call-management/verification-rule-v2`, and `/design-system` returned 200 on local dev servers 5173 and 5175.
- Source scan: no strict failure field or old strict failure UI copy remains under `src/`.
- Browser screenshot verification: not completed because the in-app browser connection timed out after 30 seconds.
