# Page State - 2026-05-28 19:36 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## livechat2 Conversation

- Message Record 入口位于 composer 工具栏，紧跟 File 图标。
- 消息区直接滚动展示历史消息和当前消息，不显示加载更多入口。
- 客户端欢迎语系统消息在坐席端隐藏。
- 已撤回消息显示 `Message recalled`，不显示 `Quote`。
- 当前坐席已撤回消息提供 `Re-edit`，点击后把原消息带回输入框。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，仍保留既有 chunk size warning。
- Browser `/`：可加载 Home tab。
- Browser `/design-system`：可加载 Design System。
