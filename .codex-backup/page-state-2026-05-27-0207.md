# Page State - 2026-05-27 02:07 +08:00

## 路由

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> 重定向到 `/`

## Modal 状态

- 通用 `.aicc-modal` 当前为浅蓝标题栏、灰蓝 body、白色内容面。
- `.aicc-modal-section` 当前有 12px padding、轻边框和圆角；`.aicc-modal-section--flush` 用于完整工作区类弹框，保留白色工作区和轻边框。
- Transfer / Outbound toolbar 控件统一 30px 高度。
- Transfer 行内动作按钮统一为 82px x 28px；Conversation 长动作按钮统一为 132px x 28px。
- Internal Chat 使用单个白色工作区，左侧列表为轻灰蓝背景，消息区为轻灰底，composer 贴合工作区底部。
- `/design-system` 的 Modal preview 已同步浅蓝标题栏和灰蓝 body。

## 验证记录

- `npm run lint`：通过。
- `npm run build`：通过。
- Browser `/`：Internal Chat 可打开。
- Browser `/`：PSTN 通话中 Transfer 可打开，`Transfer Agent` / `Transfer Skill` / `Transfer Number` 均存在，`Transfer Number` 无 `Cancel`。
- Browser `/`：Outbound Call 可打开，Call Number 为输入框 + Call 单行布局，无 `Cancel`。
- Browser `/design-system`：页面正常加载。

## 已知限制

- 本轮不修改业务流程、tab 数量、mock 数据、store、路由或话务状态机。
- Codex Browser 截图输出偶发重复拼接画面，不影响当前 DOM 与交互验证结论。
