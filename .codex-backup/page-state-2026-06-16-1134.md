# Page State - 2026-06-16 11:34 +08:00

## Routes

- `/` returned HTTP 200 during smoke validation.
- `/call-management/verification-rule-v2` returned HTTP 200 during smoke validation.
- `/design-system` returned HTTP 200 during smoke validation.

## Inbound Customer Verification

- `CustomerInformationCard` passes V2 initial conditions into `CustomerVerificationV2Modal`.
- Customer segment is now derived from `profile.customerType` instead of being fixed as `regular`.
- Priority and Solitaire profiles should match Prio/Soli rules when the skill queue is Prio/Soli.

## Rule Matching

- Exact match remains `Channel + Skill Queue + Customer Segment`.
- Perbankan default fallback still applies only when the selected skill has no exact configured KBV rule for the current channel and customer segment.
- Prio/Soli menu names are now mapped to Prio/Soli skill queue codes before the generic fallback.

## Verification

- TypeScript, lint, build, HTTP smoke, and sensitive-word scan passed.
