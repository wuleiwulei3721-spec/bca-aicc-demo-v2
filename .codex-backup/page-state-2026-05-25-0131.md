# Page State - 2026-05-25 01:31 +08:00

## Routes

- `/`: `BasicLayout` + `AgentWorkspace`
- `/design-system`: `BasicLayout` + `DesignSystem`

## Workspace Tabs

- All workspace tabs now use `WorkspaceTabLabel` for consistent icon/text spacing.
- `home`: static Home label.
- `bankapp-demo`: static BankApp Demo label.
- `whatsapp-demo`: static WhatsApp Demo label.
- `live-chat`: fixed tab after Sign In; label is `Live Chat` when no active session and `Live Chat (mm:ss)` when active sessions exist.
- `inbound`: reused for PSTN and BankApp Voice; label is `PSTN (mm:ss)` for PSTN and `Voice Call (mm:ss)` for BankApp Voice while timing is active.
- `video-call`: label is `Video Call (mm:ss)` while timing is active.

## Live Chat

- Only active sessions are visible; Webchat remains hidden.
- BankApp Live Chat adds `live-chat-002` and starts runtime timing at `00:00`.
- WhatsApp Demo adds `live-chat-001` and starts runtime timing at `00:00`.
- Customer list expanded state shows `lastMessageTime · runtime duration` for every active customer, including one-customer lists.
- Conversation header timer shares the same runtime elapsed value as the list and tab.
- SLA warning remains 60 seconds and breach remains 120 seconds; warning visuals now use amber/yellow instead of dark brown.
- End Service clears the selected session timing; with no active sessions, Live Chat tab returns to no duration.

## Customer Information

- `accessDuration` is still static channel access time, not live agent service runtime.
- The access strip now displays `Channel · accessDuration`, for example `PSTN · 05:23`, `BankApp · 02:11`, or `WhatsApp · 00:48`.
- The previous separate clock icon and standalone duration text are removed from the access strip.

## Out Of Scope

- Hang Up does not keep old inbound/video tabs as separate post-service tabs for new simultaneous calls.
- Multi inbound interaction tabs are planned for `v0.6.0` because they require store structure, tab key, data source, and close lifecycle changes.

## Validation

- `npm run lint`: passed.
- `npm run build`: passed with existing chunk size warning.
- Browser smoke `/` and `/design-system`: passed.
