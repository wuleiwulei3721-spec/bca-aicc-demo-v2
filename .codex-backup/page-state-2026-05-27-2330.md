# Page State - 2026-05-27 23:30 +08:00

## Internal Chat Modal

- 左侧 Agent Sessions、搜索和会话列表保持不变。
- 右侧 conversation header 和消息列表保持不变。
- 底部 composer：
  - 仅显示无边框 textarea。
  - 仅显示文本 `Send` 按钮。
  - 不显示 Emoji 按钮。
  - 不显示 Upload image 按钮。
  - Send 按钮不显示图标。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过。
- Browser DOM 检查：Internal Chat 弹框中无 `Emoji` / `Upload image`，存在 textarea 和 `Send` button。
