# Current TODO - 2026-05-23 19:36 +08:00

## P0

- Browser verify the collapsed and expanded left menu only exposes `PSTN`, `BankApp`, and `WhatsApp` under `Channel Simulation`.
- Browser verify `PSTN` still opens the existing phone incoming popup when the agent is Ready and Idle.
- Browser verify `BankApp` opens the existing BankApp Demo tab.
- Browser verify `WhatsApp` opens a new WhatsApp Demo tab and its Live Chat handoff focuses the WhatsApp session.

## P1

- Replace the copied WhatsApp customer-side screenshots with WhatsApp-native demo surfaces when the user provides the final desired flow.
- Decide whether a hidden debug entry is needed for ordinary Video Call after removing it from the visible menu.

## P2

- Consider extracting the shared BankApp/WhatsApp simulator shell into a neutral customer simulator component if more channels are added.
