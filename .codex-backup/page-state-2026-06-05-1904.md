# Page State - 2026-06-05 19:04 +08:00

## Branch

- `codex/customer-identity-refresh`

## Formal Live Chat

- `LiveChat2Page` passes the selected session customer directly into the shared `InteractionWorkspace`.
- Customer Information channel tag uses `activeSession.customer.accessDuration`.
- `activeSession.elapsedSeconds` still drives service-time UI:
  - Workspace tab duration.
  - Live Chat customer row duration.
  - Conversation header service duration.
  - SLA / unanswered timers.
- Handoff sessions cloned by `createLiveChat2HandoffSession()` preserve the source customer's access duration.

## Customer Identity Refresh

- PSTN still starts as `Unidentified Customer`.
- Customer Journey and Ticketing History still start in unloaded empty states.
- `Refresh customer identity` and `Edit contact` both remain in the Customer Information card header.
- `Paste` fills demo ID `00000078987`.
- `Confirm` refreshes Customer Information, Customer Journey, and Ticketing History.

## Verification State

- `npm run lint` passed.
- `npm run build` passed with the existing Vite chunk size warning.
- Browser `/` passed:
  - WhatsApp handoff showed `WhatsApp 00:18` fixed in Customer Information while service timer advanced.
  - PSTN identity refresh continued to work.
- Browser `/design-system` passed.
- Dev server responded at `http://127.0.0.1:5173/`.
