# Current TODO - 2026-05-27 23:11 +08:00

## P0

- 人工复查 `Transfer Agent` 坐席列表：无 `Department` 列，`Consult` / `Transfer` / `Conference` 三个按钮完整显示。
- 人工复查 Conversation Transfer 坐席列表：`Request Transfer`、`Request Conference`、More 按钮同一行显示。
- 人工复查 `Outbound Call > Call Agent` 坐席列表：无 `Department` 列，`Call` 按钮完整显示。
- 人工复查 Transfer / Outbound 查询栏：输入框、Skill Queue 下拉框、Status 下拉框、Search 按钮高度一致且文字垂直居中。
- 发布前再次执行 `git diff --name-only main..HEAD`，确认没有 `livechat2` / `LiveChat2` 文件。

## P1

- 如果评审仍认为坐席列表拥挤，可下一步考虑把 `Employee ID` 改短为 `ID`，或把 `Extension` 改短为 `Ext.`。

## P2

- 后续可将弹框查询栏控件高度抽成 modal-level token，避免 Transfer/Outbound 独立覆盖。
