# Page State - 2026-06-15 15:48 +08:00

## Priority List Management

Route: `/call-management/priority-list`

Current state:

- Query fields: multi-select `Channel`, text `Identifier`.
- Empty query channel means All Channels; there is no real `All` option.
- Add / Batch Add channel selector is multi-select and only lists enabled Routing Config channels.
- Identifier supports phone numbers, Bank ID, social media accounts, email addresses and email domains.
- Identifier tooltip includes:
  - General examples.
  - Match rule explanation.
  - Batch examples for phone, social media and Email/Webchat.
- List columns: checkbox, No., Channel, Identifier, Match Rule, Remark, Created Date, Created By.
- Channel column displays selected channels as tags.
- Match Rule values:
  - `Exact Match` for normal identifiers and email addresses.
  - `Email Domain Match` for identifiers matching `@domain.tld`, such as `@ojk.co.id` and `@bi.go.id`.
- Default mock records now use the customer example data.
- Duplicate preview:
  - Appears inside Add / Batch Add modal when selected channels and identifiers collide with existing entries.
  - Displays Channel, Identifier, Match Rule and Existing Record.
  - Shows `Duplicate records will be skipped automatically.`
- Save behavior:
  - Skips duplicate channels automatically.
  - Saves remaining non-duplicate channels as one page record per Identifier.
  - Does not close the modal if every selected identifier/channel is already duplicated.

## Blacklist Management

Route: `/call-management/blacklist`

Current state:

- No functional changes in this snapshot.
- HTTP smoke still returns 200.

## Verification

- TypeScript, lint and production build passed.
- HTTP smoke for `/call-management/priority-list` and `/call-management/blacklist` passed.
- Browser visual/click validation is pending because Browser tools were unavailable in this turn.
