# Page State - 2026-05-21 23:01 +08:00

## 路由

- `/`：主工作台，`BasicLayout` 包裹 `AgentWorkspace`。
- `/design-system`：设计系统展示页，`BasicLayout` 包裹 `DesignSystem`。
- `*`：重定向到 `/`。

## Workspace Tab 状态

- Home tab 始终存在，不可关闭。
- 坐席 Sign In 后，Home 旁新增 `Live Chat` tab，不可关闭。
- `Live Chat` tab 在 Sign Out 后移除；如果当前 active tab 是 Live Chat，则回到 Home。
- PSTN / Voice Call tab 仍通过 `Channel Simulation > PSTN / Voice Call` 触发，仍可关闭。
- Video Call tab 仍通过 `Channel Simulation > Video Call` 触发，仍可关闭。

## Live Chat 页面

- 页面组件：`src/pages/inbound/LiveChatPage.tsx`。
- 客户列表组件：`src/pages/inbound/components/LiveChatCustomerList.tsx`。
- 共用三栏容器：`src/pages/inbound/InteractionWorkspace.tsx`。
- 左侧客户列表展示 WhatsApp、Haloapps、Webchat 会话，可收起展开。
- 右侧继续显示 Customer Information、CRM workspace、Assistant / Connection 三栏。
- 切换客户会更新 Customer Information 中的客户、渠道、时长与验证状态。
- 文字聊天渠道的 Call Flow Detail 不显示 IVR Journey。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，保留既有 Vite chunk size warning。
- Browser `/`：Sign In 后出现不可关闭 Live Chat tab；客户列表切换和收起通过。
- Browser `/design-system`：页面正常加载。
