# Page State - 2026-05-25 17:51 +08:00

## 路由

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> 重定向到 `/`

## Inbound 弹屏状态

- `Ticketing History` 继续使用 v0.6.7 的右侧 hover overlay 箭头，日期默认不被箭头占位影响。
- `Next Best Action` 仍点击打开 CRM 动态业务 tab；本轮只修正 hover 箭头占位和错位。
- `Next Best Action` 行默认只有服务文本列，hover/focus 箭头浮在最右侧上方。
- CRM tabs 更多按钮保持 v0.6.6 的紧凑宽度与图标居中。

## 验证记录

- `npm run lint`：通过。
- `npm run build`：通过。
- Browser `/`：主路由正常加载。
- Browser `/design-system`：页面正常加载。

## 已知限制

- 本轮不改变 PSTN、BankApp Voice、BankApp Video、Live Chat 或 Ready-aware handoff 行为。
- 仍建议用客户目标分辨率人工确认 Next Best Action hover 箭头效果。
