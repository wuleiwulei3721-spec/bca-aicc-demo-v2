# Current TODO - 2026-06-03 13:21 +08:00

## P0

- Manual review `Routing Config > Skill Routing Rules`: confirm the generated preview table remains readable when the enabled route factor count grows beyond the current 5 fields.

## P1

- Confirm backend representation for unrestricted route factors if this demo is connected to a real API.
- Replace demo-only `updatedBy = Admin` with real authenticated user audit data when backend integration is introduced.

## P2

- If the product removes priority entirely, delete the hidden `priority` field from route rule data after confirming no matching/ranking behavior depends on it.
