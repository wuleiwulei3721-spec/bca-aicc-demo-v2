# Page State - 2026-05-22 15:52 +08:00

## Routes

- `/`: `BasicLayout` -> `AgentWorkspace`.
- `/design-system`: `BasicLayout` -> `DesignSystem`.
- `*`: redirects to `/`.

## Live Chat Workspace

- Agent Sign In adds fixed, non-closable `Live Chat` workspace tab beside Home.
- Live Chat still uses the four-column layout: customer list, Customer Information, CRM workspace, Assistant.
- Customer list still supports collapsed/expanded states and ALL / WhatsApp / BankApp / Webchat filtering.
- Selecting a customer updates Customer Information and the new Conversation tab content.

## Conversation Tab

- `Conversation` is a fixed, non-closable tab immediately to the right of `CRM` inside the CRM workspace.
- Live Chat opens with `Conversation` selected by default.
- Top header displays the selected customer and intent/duration, with `Transfer`, `Invite`, and `End Service` buttons.
- Middle content displays customer and historical agent messages from the selected `LiveChatSession.conversation`.
- Composer contains a message textarea plus emoji, file, and send controls.
- Sending a message appends a Current Agent bubble and updates the selected customer's list row last message.
- `End Service` opens a confirmation modal. Confirming closes the selected customer and switches to the next available customer.

## Validation

- `npm run lint`: passed.
- `npm run build`: passed with existing chunk size warning.
- Browser `/`: signed in, opened Live Chat, verified fixed Conversation tab, customer switching, message send, and End Service confirmation.
- Browser `/design-system`: page loaded normally.
