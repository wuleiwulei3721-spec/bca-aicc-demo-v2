# Page State - 2026-06-16 16:02 +08:00

## Inbound Workspace

- `/` still renders `BasicLayout` and `AgentWorkspace`.
- PSTN and BankApp voice/video call interactions create a `CallInteraction` with `skillDisplayName`.
- Top call toolbar call context now includes Skill during active call lifecycle states:
  - Incoming
  - Talking
  - Hold
  - Mute
- Idle and ended calls do not show Skill in the toolbar.
- Customer Information still contains the existing `Menu` route hint as secondary supporting context.

## Design System

- `/design-system` route remains reachable.
- No design-system component APIs were changed.

## Validation State

- `npx tsc --noEmit --pretty false` passed.
- `npm run lint` passed.
- `npm run build` passed with the existing Vite/Rolldown chunk-size warning.
- HTTP smoke for `/` and `/design-system` returned 200.
- Browser screenshot/DOM verification remains pending due Browser plugin connection timeout and missing local Playwright/Puppeteer packages.
