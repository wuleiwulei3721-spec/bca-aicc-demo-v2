# Page State - 2026-05-28 16:20 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## Workspace Tabs

- `Home`：默认 tab。
- `BankApp Demo` / `WhatsApp Demo`：可关闭 demo tabs。
- `Live Chat`：固定 tab，显示最长 live chat 服务计时并可短闪。
- `livechat2`：固定 tab，当前新增最长 livechat2 服务计时与新接入短闪。
- `PSTN` / `Voice Call` / `Video Call`：多通话动态 tabs，显示通话计时，active call 不可关闭，ended 后冻结计时并可关闭。

## livechat2 Tab Behavior

- `durationStartedAt` 来自 active livechat2 客户中服务时间最长的一位。
- 如只剩 ended 未关闭会话，则使用 ended 会话的 `endedAt` 冻结显示。
- `isFlashing` 来自 active livechat2 timings 的最大 `flashUntil`。
- `slaState` 沿用 active livechat2 timings 的运行态计算。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，保留既有 chunk size warning。
- Browser `/`：可加载 Home tab。
- Browser `/design-system`：可加载 Design System。
- livechat2 菜单点击与 tab 计时/闪烁仍需人工复查。
