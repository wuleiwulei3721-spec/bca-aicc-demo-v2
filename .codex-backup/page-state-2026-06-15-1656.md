# Page State - 2026-06-15 16:56 +08:00

## Shared Management Form Rule

Affected style area: `src/styles/index.less`

Current state:

- Single-line `.ant-input:not(textarea)` fields are fixed to the management control height.
- `textarea.ant-input` is not forced to 32px.
- Generic textareas have `height: auto`, `min-height: 76px`, and vertical resize enabled.

## Blacklist Management

Route: `/call-management/blacklist`

- Batch Add Restricted Number textarea uses `rows={8}`.
- Batch textarea min-height is 176px.
- It should no longer render as a one-line input.

## Priority List Management

Route: `/call-management/priority-list`

- Batch Add Identifier textarea uses `rows={8}`.
- Batch textarea min-height is 176px.
- It should no longer render as a one-line input.

## Verification

- TypeScript, lint and production build passed.
- HTTP smoke for both affected routes passed.
- Browser visual check is pending.
