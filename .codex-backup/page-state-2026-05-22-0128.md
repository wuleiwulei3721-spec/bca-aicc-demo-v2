# Page State - 2026-05-22 01:28 +08:00

## Routes

- `/`: main workspace through `BasicLayout` and `AgentWorkspace`.
- `/design-system`: design system page through `BasicLayout`.
- `*`: redirects to `/`.

## Live Chat Customer List

- Page: `src/pages/inbound/LiveChatPage.tsx`.
- Panel component: `src/pages/inbound/components/LiveChatCustomerList.tsx`.
- Customer list is still default collapsed.
- Filter controls are icon-only and support hover titles.
- Default filter state selects WhatsApp, BankApp, and Webchat.
- ALL is highlighted only when all three individual channels are selected.
- Clicking ALL selects all individual channels.
- Clicking WhatsApp, BankApp, or Webchat toggles that channel on/off.
- Inactive channel filter icons are grey; active icons use channel color.
- Webchat icon uses orange, WhatsApp uses green, BankApp uses blue.
- Panel background is deeper cool grey-blue to separate the customer list column from the rest of the Live Chat workspace.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with existing Vite chunk size warning.
- Browser `/`: passed for Sign In, Live Chat tab, default all-channel selected state, deselecting Webchat, ALL state cancellation, and ALL reselecting every channel.
- Browser visual check `/`: passed for deeper panel background and Webchat icon color difference.
