# Current TODO - 2026-05-27 23:30 +08:00

## P0

- 人工复查 Internal Chat 弹框底部：只显示输入区和 `Send` 按钮。
- 确认 Internal Chat 底部不再显示 Emoji、Upload image 或 Send 图标。
- 确认输入框默认态和 focus 态不显示边框或阴影。
- 发布前再次执行 `git diff --name-only main..HEAD`，确认没有 `livechat2` / `LiveChat2` 文件。

## P1

- 如果用户希望输入区更紧凑，可将 textarea `minRows` 从 2 调整为 1。

## P2

- 后续可统一 Live Chat / Internal Chat composer 的输入区规则，避免两个聊天输入区视觉口径分散。
