# Page State - 2026-05-28 20:10 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## livechat2 Conversation

- Composer 工具栏图标：Emoji、File、Message Record。
- 点击 `Message Record` 图标后，右侧 Assistant 区域新增 `Message Record` tab，并自动切换到该 tab。
- `Message Record` tab 展示当前客户消息记录，保留筛选、搜索和记录数量。
- Conversation 中间区域只展示聊天消息，不再渲染内嵌历史记录侧栏。

## Right Panel

- 默认页签仍为 `Assistant` 与 `Connection`。
- `livechat2` 可向右侧注入 `Message Record` 额外页签。
- 额外页签可关闭，关闭后 active tab 回到 `Connection`。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，仍保留既有 chunk size warning。
- Browser `/`：页面可加载，Home tab 正常。
- Browser `/design-system`：页面可加载，Design System 文本可见。
- Browser livechat2 点击链路：当前 in-app browser 可见 DOM 未暴露左侧菜单项，临时桌面 viewport 后仍只能看到 Home tab，需人工复查。
