# Page State - 2026-06-10 18:17 +08:00

## Routes

- `/login` remains the public login page.
- `/`, `/design-system`, and visible `/call-management/*` routes still require auth session.
- `/routing-config/*` remains hidden from the customer main line and redirects back to `/`.

## Header And Agent Status

- System `Log Out` remains the independent red power button in the header.
- Unsigned profile dropdown shows `Sign In` with `Voice only`, `Digital only`, and `Voice + Digital`.
- Ready / Not Ready signed-in dropdown shows current service mode, `AUX` group, enabled busy reasons, and media `Sign Out`.
- `AUX - {reason}` dropdown shows:
  - signed-in service mode;
  - current status line;
  - `Ready`;
  - `Sign Out`.
- `Pre-AUX - {reason}` dropdown shows the same reduced structure and does not show other AUX reasons.

## Pre-AUX Interaction

- Selecting an AUX reason with active customer work enters Pre-AUX.
- Pre-AUX keeps current call / Live Chat sessions intact.
- Pre-AUX blocks new voice/video and digital handoff because readiness remains unavailable unless status is `Ready`.
- Clicking `Ready` cancels pending AUX.
- Clicking `Sign Out` opens the existing media sign-out confirmation and can return directly to `Unsigned`.
- When all active work ends, Pre-AUX becomes final AUX automatically.

## Verification State

- `npx tsc --noEmit` passed.
- `npm run lint` passed.
- `npm run build` passed with existing Vite chunk size warning.
- Temporary 5177 headless Chrome/CDP smoke passed for Ready AUX menu, reduced AUX menu, Sign Out confirmation availability, and Voice + Digital Pre-AUX.
