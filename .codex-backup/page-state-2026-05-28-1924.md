# Page State - 2026-05-28 19:24 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## Transfer Modal

- Conversation 入口打开 Transfer 弹框后，`Transfer Agent` 表格行内只显示 `Transfer` / `Conference`。
- 更多按钮和 `Force Transfer` / `Force Conference` 下拉入口已移除。
- Conversation actions 列宽为 180px。
- 通话工具条 Transfer 弹框原有 `Consult` / `Transfer` / `Conference` 仍保留。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，仍保留既有 chunk size warning。
- Browser `/`：可加载 Home tab。
- Browser `/design-system`：可加载 Design System。
