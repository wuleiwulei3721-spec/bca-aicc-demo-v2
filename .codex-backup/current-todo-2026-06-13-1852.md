# Current TODO - 2026-06-13 18:52 +08:00

## P0

- Manually open `Edit Verification Rule V2` and confirm each special scenario row shows `Scenario`, `Mode`, `Questions`, `Correct`, and delete in one compact row.
- Confirm `Branch Combined Verification` is `Replace`.
- Confirm `Khusus laporan mbl d` is `Replace`.

## P1

- In the customer verification popup, select `ATO / add-on` and confirm base questions remain with appended special questions.
- Select `Branch Combined Verification` and confirm only the six special questions are shown.
- Switch from `Replace` back to `None` or an `Append` scenario and confirm only currently visible question answers are retained.

## P2

- If customer asks for multiple special scenarios at once, define merge priority before implementation.
- If customer asks for separate branch/customer thresholds, add special scenario subgroups instead of overloading the single `Correct` field.
