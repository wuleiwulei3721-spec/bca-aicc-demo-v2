# Current TODO - 2026-06-05 00:49 +08:00

## P0

- Manually check the sidebar in expanded and collapsed flyout states: `Call Management` should show `全局控制配置` above `Text Channel Settings`.
- Manually check `/call-management/global-control-configuration`:
  - Default Answer Mode is Auto Answer.
  - Default Answer Duration is 3 seconds.
  - Switching to Manual Answer hides Answer Duration.
  - Default Sign-in Status is Ready.
  - Auto Cancel ACW Duration is 5 seconds.
  - Idle Auto Sign-out Duration is 30 minutes.
  - Idle Warning Lead Time is 10 minutes.
  - Max Text Media Services is 3.
  - Save and Restore Defaults work.
- Confirm with stakeholders whether the page should remain Chinese in local demo or eventually move to English like the Routing Config pages.

## P1

- Decide whether global control configuration should be wired into a mock store shared with `BasicLayout`, agent sign-in state, ACW state, and text media capacity rules.
- Define backend/API contract if these settings need to become real configurable runtime behavior.
- Decide whether there are multiple profiles or only one default global configuration; current implementation is one default configuration page.

## P2

- Consider adding field-level help text for which settings are default-only once the real profile/default model exists.
- Consider adding audit metadata such as Updated Date / Updated By if this becomes a real admin page.

## Still Relevant Existing Checks

- Production `main` still hides `Call Management` and `Routing Config`; do not merge this local admin menu state to customer Production without an explicit release decision.
- Continue manual regression for `Routing Config > Channels`, `Channel Types`, `Skill Queues`, and old URL redirects after large routing changes.
