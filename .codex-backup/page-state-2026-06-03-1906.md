# Page State - 2026-06-03 19:06 +08:00

## Channels

- Page now has the standard `.routing-config-page` root wrapper.
- Query toolbar, table, pagination, and action buttons are under the same admin styling scope as ordinary CRUD pages.
- `Rule Plan` column remains visible.

## Media Service Rule Plans

- Page now has the standard `.routing-config-page` root wrapper.
- Query toolbar, table, pagination, and action buttons are under the same admin styling scope as ordinary CRUD pages.
- `Key Rules` remains removed.

## Modal State

- Complex modal sections inherit `--routing-config-control-height: 32px`.
- Modal shell, footer, delete alert, and validation alert remain aligned with ordinary CRUD pages.
