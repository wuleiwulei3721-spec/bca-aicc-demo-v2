# Page State - 2026-05-21 12:03 +08:00

## 路由

- `/`：主工作台，`BasicLayout` 包裹 `AgentWorkspace`。
- `/design-system`：设计系统展示页，`BasicLayout` 包裹 `DesignSystem`。
- `*`：重定向到 `/`。

## 页面状态

- Header 显示 `BANK 1`，并恢复旧版蓝色渐变顶部栏。
- Browser Title 为 `BANK 1 AICC Demo`。
- App/Workspace 主背景恢复旧版浅色体系。
- Customer Information 高亮卡片恢复旧版浅蓝层级。
- Ticketing History、Next Best Action、Customer Journey 等主页面卡片恢复旧版白底和浅 divider 风格。
- Agent Toolbar 恢复蓝色 Header 上的半透明话务条和深蓝 active/selected 状态。
- CRM/Assistant 区域继续使用代码内 BANK 1 fallback，不加载公开旧品牌截图。
- Modal/Dialog 本轮未继续调整。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，保留既有 Vite chunk size warning。
- `http://127.0.0.1:5175/`：HTTP 200。
- 旧品牌可见文案扫描：未发现残留。
- 浏览器截图级验证：当前 Codex in-app browser pane 不可用，后续需要补做。
