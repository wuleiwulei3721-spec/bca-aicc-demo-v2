# Page State - 2026-05-21 15:39 +08:00

## 路由

- `/`：主工作台，`BasicLayout` 包裹 `AgentWorkspace`。
- `/design-system`：设计系统展示页，`BasicLayout` 包裹 `DesignSystem`。
- `*`：重定向到 `/`。

## 全局布局状态

- `BasicLayout` 顶部 Header、Agent Toolbar、Agent Profile、Notifications、Internal Chat 入口保持原逻辑。
- 左侧系统菜单默认收起，收起宽度为 `48px`。
- 展开宽度继续使用 `--aicc-layout-sider-width`，当前为 `220px`。
- 展开态顶部为 Collapse 按钮 + `Search menu` 输入框。
- 收起态顶部只显示 Expand 按钮，不显示搜索框。

## 左侧菜单状态

- 展开态一级菜单显示图标和英文文字。
- 展开态点击含子项的一级菜单会在下方展开二级菜单。
- 展开态搜索会过滤一级和二级菜单；匹配二级菜单时自动展示对应父级。
- 二级菜单仅显示文字，不配置图标。
- 收起态仅显示一级菜单图标，图标水平居中。
- 收起态含二级菜单的一级项支持右侧浮层选择二级菜单。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，保留既有 Vite chunk size warning。
- `/`：已通过 Browser 检查默认收起、展开按钮、搜索过滤和收起后搜索隐藏。
- `/design-system`：已通过 Browser 检查页面可访问。
