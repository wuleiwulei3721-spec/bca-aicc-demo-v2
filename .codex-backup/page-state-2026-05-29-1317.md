# Page State - 2026-05-29 13:17 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## livechat2 Right Panel

- Base tabs remain `Assistant` and `Connection`.
- `Quick Replies` is a fixed livechat2-only tab after `Connection`.
- `Message Record` remains a dynamic closable tab appended after `Quick Replies` when opened from the composer.

## Quick Replies

- Search filters by code and phrase text.
- `My Phrases` and `Public Phrases` are expanded by default and independently collapsible.
- `My Phrases` supports runtime add/rename/delete groups and add/edit/delete phrases.
- Deleting a My group deletes its phrases.
- `Public Phrases` is read-only.
- Clicking a phrase inserts it into the current livechat2 composer and moves the caret to the end.
- Conversation `/` popup uses the same runtime phrase data.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with the existing Vite/Rolldown chunk size warning.
- Browser `/` and `/design-system`: loaded successfully.
- Actual livechat2 Quick Replies flow: still needs manual review because the in-app browser DOM did not expose the `livechat2` menu entry.
