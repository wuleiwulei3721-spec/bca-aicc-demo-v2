# Current TODO - 2026-06-15 11:47 +08:00

## P0

- None known after the V2 scenario model refactor.

## P1

- Manually inspect `/call-management/verification-rule-v2` in browser if browser-use becomes available, especially Add/Edit rule, scenario switch, question picker, and modal density.
- Manually verify `Customer Verification` popup scenario switch with `Layanan Cabang`:
  - `Default` shows branch questions and needs 2 correct.
  - `Branch to Other Services` shows branch + customer data blocks and needs 3 + 3 correct.
  - Same question answers remain when switching scenarios.
- Confirm with customer whether `Organization Segment Override` should remain a rule-level override or become its own scenario in a later iteration.

## P2

- Consider removing legacy `VerificationV2SpecialScenarioMode` exports after all mock/store data has been migrated and no compatibility path is needed.
- Consider scenario-level `Max Wrong` only if customer explicitly needs different failure thresholds by scenario.
- Consider drag sorting for question lists if the question volume grows; current implementation keeps arrow-based sorting.
