# Current TODO - 2026-06-03 18:26 +08:00

## P0

- Continue user review of `Routing Config > Channels` and `Media Service Rule Plans`.
- Confirm whether `Text Channel Settings` should be deleted from `Call Management` now or in a later pass.
- Manually review Add/Edit/Delete flows after more detailed business feedback, especially Text plan validation and channel binding defaults.

## P1

- Decide whether Voice / Video need their own service rule plan pages or a shared generic media rule framework.
- Decide whether queue alert rules should support all Text channels or only Haloapp / webchat / WhatsApp.
- Decide whether Webchat Recall Limit belongs in a Text plan shared by all channels or in a webchat-only override plan.

## P2

- Consider automated smoke tests for Routing Config CRUD pages.
- Consider splitting `RoutingConfigDataPages.tsx` into smaller page files after the routing config module stabilizes.
- Consider Vite code splitting if the existing large chunk warning becomes a deployment concern.
