# Current TODO - 2026-06-05 10:33 +08:00

## P0

- Manually review `Routing Config > Skill Queues`:
  - Add/Edit Work Time Plan dropdown shows only one `Default 24x7`.
  - Selecting `Default 24x7` does not show `Preview`.
  - Selecting `Bank Working Hours` shows `Preview`.
  - Clicking `Preview` opens `View Working Time Plan`.
  - Closing preview returns to the Skill Queue modal without losing form context.
  - View mode for a queue with a non-default Work Time Plan also shows `Preview`.
- Regress `Routing Config > Working Time Plans`:
  - Existing Add/View/Edit/Delete modals still open normally.
  - The original Working Time Plan maintenance page remains editable; the Skill Queue preview is read-only only.

## P1

- If the same duplicate `Default 24x7` issue appears in other pages, inspect whether those pages also prepend a default option on top of `useRoutingLookups().workTimeOptions`.
- Decide whether future preview modals should support a direct jump to Working Time Plan edit, or stay read-only for safer configuration review.

## P2

- Consider adding a small automated smoke test for Routing Config modals if this demo keeps growing.
