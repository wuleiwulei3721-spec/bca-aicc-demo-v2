# Current TODO - 2026-06-06 16:35 +08:00

## P0

- Manually verify `Sign In -> PSTN -> Verify` in the browser:
  - no long statistics string after Business Type;
  - rule bar shows `Need 5 correct`;
  - Mandatory / Dynamic / Static blocks update color and counts;
  - `Wrong -> Correct` and `Skip -> Correct` update statistics without `Clear All`;
  - `Apply Verified` is enabled after required groups are met.
- Manually verify config-to-modal linkage:
  - edit `Phone + Perbankan` in `Call Management > Verification Rules`;
  - change a required count or correct threshold;
  - reopen PSTN verification modal and confirm the new rule appears.
- Confirm customer-facing rules:
  - whether `Berurut` means strict question order;
  - whether successful HaloApp PIN equals `HaloApp Registered`;
  - whether authenticated-entry reductions apply to WhatsApp, Webchat, Video, etc.;
  - whether agents may change business type and whether a reason must be recorded.
- Confirm production answer source and matching:
  - CRM / Card Link / CardPack / Base24 / HaloApp login state / AICC static config;
  - whether agents can ever see standard answers.
- Confirm audit and failure handling:
  - wrong-answer limit workflow;
  - verification record retention fields;
  - ATO, add-on, O1-O5, mbl d, KBB, BBP triggers;
  - whether Paylater uses Perbankan rules or an independent rule.

## P1

- Decide whether `Verification Rules` needs search/filter or add/delete controls for later demos.
- Decide whether rule edits should be import/export-able if customer wants repeated demo sessions without losing changes on refresh.
- Consider replacing deprecated Ant Design `destroyOnClose` usage with `destroyOnHidden` across modal components.

## P2

- Add automated smoke tests for key routes and verification modal state transitions.
- Consider route-level code splitting if Vite chunk size warning becomes a deployment concern.
