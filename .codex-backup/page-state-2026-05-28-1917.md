# Page State - 2026-05-28 19:17 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## Transfer Modal

- Conversation 入口打开 Transfer 弹框后，`Transfer Agent` 表格动作列宽为 198px。
- Conversation 行内主按钮为 `Transfer` / `Conference`，按钮宽度为 80px。
- Transfer 弹框表头不换行。
- 更多菜单仍保留 `Force Transfer` / `Force Conference`。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，仍保留既有 chunk size warning。
- Browser `/`：可加载 Home tab。
- Browser `/design-system`：可加载 Design System。
