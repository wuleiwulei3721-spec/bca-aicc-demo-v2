# Current TODO - 2026-06-04 00:28 +08:00

## P0

- User visual review of `Skill Routing Rules` toolbar: `Batch Add` should be independent on the right, not adjacent to `Reset`.
- Check the toolbar at desktop and narrower widths to confirm wrapping remains readable.

## P1

- Continue enforcing ordinary admin toolbar structure for all custom Routing Config pages.
- Avoid placing page-level Add/Batch Add buttons inside the filters container.

## P2

- Consider a static lint-style check for custom admin pages that require `query-group` and right-side `add-action`.
