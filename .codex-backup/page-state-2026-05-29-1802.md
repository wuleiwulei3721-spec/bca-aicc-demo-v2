# Page State - 2026-05-29 18:02 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## Live Chat

- Official `Live Chat` tab renders `LiveChat2Page`.
- Demo handoff creates new runtime sessions and their service timers start at `00:00`.
- Customer Information receives the active session runtime access duration.
- Customer list default star color is gray `No flag`.
- No-flag rows let the latest message occupy the full row; the gray star hover target floats at the right edge.

## CRM / Assistant Tabs

- Assistant, Connection, Quick Replies, and Message Record use a flat compact label structure.
- CRM and Assistant more buttons are narrowed and close buttons have higher stacking priority.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with the existing Vite/Rolldown chunk size warning.
- Browser `/`: loaded and Sign In showed the fixed `Live Chat` tab; detailed Live Chat interaction still needs manual review.
- Browser `/design-system`: loaded with `UI Design System` and `Color System` visible.
