# Page State - 2026-06-06 15:47 +08:00

## `/`

- 主工作台、PSTN、BankApp Demo、Live Chat 和话务工具条保持既有状态。
- Customer Information 的 `Verify` 打开紧凑版 Customer Verification Assist。

## Customer Verification Assist

- 顶部一行展示：
  - `Channel Type`
  - `Business Type`
  - `Correct/Wrong/Skip/Status`
- 规则与进度合并展示，例如 `Need 5 correct: 1 Mandatory, 2 Dynamic, 2 Static`。
- 详细规则说明通过问号 popover 查看。
- 题目行不展示标准答案、`Demo answer`、`Source` 或答案来源。
- 每题的 `Correct / Wrong / Skip` 可重复点击并互相覆盖。
- 达到失败上限后仍可在 Apply 前修正题目；修正后状态可恢复进行中。
- `Clear All` 清空整次验证进度，不作为误点修正主路径。

## BankApp / HaloApp PIN

- BankApp Voice 未 PIN 时，验证弹窗显示紧凑 `PIN required` 和 `Send PIN Verification`。
- 发送 PIN 后客户侧 BankApp Demo 展示 4 位 PIN 页面。
- 客户提交 PIN 后，坐席侧重新打开验证弹窗显示 `HaloApp Registered` 并加载紧凑题库。

## `/design-system`

- 本轮未改 Design System 结构。
- Browser smoke check 正常加载 `UI Design System`。

## 验证命令

- `npm run lint`：通过。
- `npm run build`：通过，仍只有既有 Vite chunk size warning。
