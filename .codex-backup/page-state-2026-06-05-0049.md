# Page State - 2026-06-05 00:49 +08:00

## Route And Menu

- Page route: `/call-management/global-control-configuration`.
- Menu path: left sidebar `Call Management` -> `全局控制配置`.
- Old compatibility route `/call-management/routing-configuration` still redirects to `/routing-config/route-elements`.

## Page Layout

- Page title: `全局控制配置`.
- Eyebrow: `Call Management`.
- Header actions:
  - Last saved timestamp text.
  - `恢复默认`.
  - `保存配置`.
- Notice appears after successful save.

## Sections

- `应答配置`
  - `应答方式`: select, required, values `自动应答` / `手动应答`.
  - `应答时长`: number, seconds, required only when `自动应答` is selected.
- `签入与话后整理`
  - `签入后状态`: select, required, values `就绪` / `未就绪`.
  - `自动取消话后整理态时长`: number, seconds, required.
- `久不操作控制`
  - `久不操作界面自动签出时长`: number, minutes, required.
  - `久不操作界面提前预警时长`: number, minutes, required.
- `文字媒体容量`
  - `文字媒体最大服务数`: number, count, required.

## Validation

- Required select fields must have a value.
- All numeric fields must be greater than 0.
- Idle warning lead time must be less than idle auto sign-out duration.
- Answer duration is hidden and not validated when answer mode is manual.
- Save button is disabled while validation errors exist.

## Verification State

- Lint passed.
- Build passed with existing chunk size warning.
- Browser check confirmed render, manual-answer field hiding, and save notice.
