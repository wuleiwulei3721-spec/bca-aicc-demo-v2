# Page State - 2026-05-21 19:03 +08:00

## 路由

- `/`：主工作台，`BasicLayout` 包裹 `AgentWorkspace`。
- `/design-system`：设计系统展示页，`BasicLayout` 包裹 `DesignSystem`。
- `*`：重定向到 `/`。

## Workspace Tabs

- Home：默认常驻 tab。
- Inbound：点击 `Channel Simulation > PSTN / Voice` 且坐席 Ready/Idle 时打开，可关闭。
- Video Call：点击 `Channel Simulation > Video Call` 且坐席 Ready/Idle 时打开，可关闭。

## Video Call 状态

- Video Call tab 内容复用 `InteractionWorkspace` 三栏布局。
- Customer Information 渠道显示为 `Haloapps`，图标为视频图标。
- OpenEye 截图浮窗来自 `public/screenshots/openeye-video-call.png`。
- OpenEye 浮窗为 fixed 高层级图片窗口，初始位于页面右侧，可拖动。
- 关闭 Video Call tab 后 OpenEye 浮窗消失。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，保留既有 Vite chunk size warning。
- `/` Browser smoke check：Video Call 触发、Answer 可用、Haloapps 渠道、OpenEye 浮窗显示/拖动、tab 关闭隐藏浮窗均通过。
- `/` Browser smoke check：PSTN/Voice 电话弹屏仍可触发。
- `/design-system` Browser smoke check：通过基础加载。
