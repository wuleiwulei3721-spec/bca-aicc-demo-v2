# Page State - 2026-06-15 16:06 +08:00

## Routes

- `/call-management/verification-rule-v2`: Verification Rule V2 list and edit modal.
- `/`: Customer Information `Verify` still opens the V2 customer verification popup.
- `/design-system`: admin component examples remain available.

## Verification Rule V2

- List behavior is unchanged.
- Edit/Add modal:
  - Fixed maximum height.
  - Scrollable content body.
  - Fixed `Preview / Cancel / Save` footer.
  - `Channel` and `Customer Segment` multi-selects stay one line.
  - `Verification Scenario` uses compact tabs.
  - `+ Scenario` supports copying current scenario or creating blank scenario.
  - Non-default scenarios can be renamed inline and deleted.
  - Default scenario cannot be deleted.

## Customer Verification Popup

- Scenario selection behavior is unchanged:
  - Single-scenario rules hide the Scenario select.
  - Multi-scenario rules show Scenario select.
  - Switching scenario preserves answers for shared question IDs.

## Validation State

- `npx tsc --noEmit --pretty false` passed.
- `npm run lint` passed.
- `npm run build` passed with the existing chunk size warning.
- `rg -n -i "halo|bca" src` returned no matches.
- HTTP smoke for `/`, `/call-management/verification-rule-v2`, and `/design-system` returned 200.
- `git diff --check` passed with only Windows line-ending warnings.
- Browser click-level verification is still pending because Playwright is not installed and no local browser tool was exposed.
