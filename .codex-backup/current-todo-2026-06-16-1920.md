# Current TODO - 2026-06-16 19:20 +08:00

## P0

- None.

## P1

- Manually verify `/routing-config/channels` in browser:
  - Routing Config menu is visible by default.
  - Phone row `Accounts` button is disabled and does not open the account modal.
  - Webchat `Business Config > Text` shows `Webchat Message Recall Limit (sec)`.
  - Non-Webchat text channels do not show `Webchat Message Recall Limit (sec)`.
- Manually verify `/` Inbound:
  - Top toolbar still shows `IVR/BankID` and `Skill`.
  - Customer Information bottom area no longer shows `Menu`.

## P2

- Add `Connection Status` and `Test Connection` to Channels after customer confirms result detail expectations.
- Add social `Interaction Type` switches after customer confirms the per-platform type list.
- Add Email survey settings after customer confirms the survey trigger condition.
