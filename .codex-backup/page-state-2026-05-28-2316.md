# Page State - 2026-05-28 23:16 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## livechat2 Message Record

- Message Record opens as a right-side extra tab next to `Assistant` and `Connection`.
- The composer history icon is active while the tab is open and closes the tab when clicked again.
- The panel body starts directly with search controls; it no longer has an internal title or close button.
- Search controls are designed as one row: date range picker, message search input, `Search`, compact count.
- Locate behavior still targets the center Conversation message and briefly highlights it.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed, with existing Vite/Rolldown chunk size warning.
- Browser `/`: loaded, Home tab visible.
- Browser `/design-system`: loaded, Design System text visible.
- Browser livechat2 click path: not completed because the in-app browser DOM did not expose the left `livechat2` menu item.
