# Page State - 2026-06-06 16:35 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `/call-management` -> redirects to `/call-management/verification-rules`
- `/call-management/verification-rules` -> `VerificationRulesPage`
- `/call-management/text-channel-settings` -> `TextChannelSettingsPage`
- `/call-management/*` -> redirects to `/call-management/verification-rules`
- `/routing-config` -> redirects to `/`
- `/routing-config/*` -> redirects to `/`
- `*` -> redirects to `/`

## Side Menu

- Visible:
  - `Channel Simulation`
    - `PSTN`
    - `BankApp`
    - `WhatsApp`
  - `Call Management`
    - `Verification Rules`
    - `Text Channel Settings`
  - `Agent Center`
  - `Operations`
  - `Reports`
- Hidden:
  - `Routing Config`

## Verification Modal

- Top toolbar:
  - `Channel Type`
  - compact `Business Type`
  - status badge: `PIN Required`, `In Progress`, `Passed`, or `Failed`
- Rule bar:
  - `Need N correct`
  - group requirement blocks ordered Mandatory, Dynamic, Static, Alternative, Layering, Special
  - weak wrong-count hint
  - notes in question-mark popover
- Question list:
  - sequence, group, question text, action buttons
  - no demo answer
  - no answer source
  - single-question status can be changed directly before final apply

## Verification Rules Page

- Table fields:
  - Channel Type
  - Business Type
  - Required Rule
  - Question Set
  - Max Wrong
  - Layering
  - Status
  - Actions
- Modal fields:
  - Channel Type
  - Business Type
  - Status
  - Correct Required
  - Max Wrong Attempts
  - Need Layering
  - required counts by question group
  - Question Set selection
- Data is demo-only front-end store state.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed.
- Browser route checks passed for Call Management pages, redirects, and Design System.
- Manual verification still needed for full PSTN verification modal path due browser-control limitation in this session.
