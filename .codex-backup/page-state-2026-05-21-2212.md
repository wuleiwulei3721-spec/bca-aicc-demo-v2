# Page State - 2026-05-21 22:12 +08:00

## 路由

- `/`：主工作台，`BasicLayout` 包裹 `AgentWorkspace`。
- `/design-system`：设计系统展示页，`BasicLayout` 包裹 `DesignSystem`。
- `*`：重定向到 `/`。

## 左侧菜单状态

- Channel Simulation 子菜单顺序为：`PSTN / Voice Call`、`Video Call`、`Live Chat`。
- `PSTN / Voice Call` 点击后仍触发电话来电流程。
- `Video Call` 点击后仍触发视频来电流程。
- `Live Chat` 当前只作为菜单项展示和选中态入口。

## Workspace Tab 状态

- Home tab 保持不变。
- 电话弹屏 tab 用户可见文案为 `PSTN / Voice Call`，内部 key 仍为 `inbound`。
- Video Call tab 保持 `Video Call`。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，保留既有 Vite chunk size warning。
- Browser check `/`：菜单文案、菜单顺序和 `PSTN / Voice Call` tab 文案已确认。
