# Page State - 2026-06-12 13:16 +08:00

## Call Management

- `/call-management` still redirects to `/call-management/verification-rules`.
- `/call-management/verification-rules` remains the existing V1 customer-facing page.
- `/call-management/verification-rule-v2` is newly added under `Call Management > Verification Rule V2`.

## Verification Rule V2

- Top-level actions include `Question Bank`, `New Rule`, and `Reset Demo Rules`.
- Question Bank maintains question names only, with enabled / disabled state.
- Rule editor supports channel multi-select, service scenario / skill queue single-select, customer segment multi-select, status, no-limit max wrong handling, failure action, question groups, Berurut switches, and special rule templates.
- `Correct Required` is read-only and calculated from Mandatory, Dynamic, and Static required counts.
- Alternative questions are configured as backup questions and do not add their own required count.

## Inbound Verify Popup

- Customer Information `Verify` now opens the V2 verification popup.
- The popup matches enabled V2 rules by channel, skill queue, and customer segment.
- `Demo Conditions` can simulate case type, organization segment, and branch service to show special rule effects.
- Agents see question text, question group, progress, and Correct / Wrong / Skip actions only.
- Standard answers and answer sources are not displayed.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with chunk-size warning.
- HTTP smoke: `/`, `/call-management/verification-rules`, `/call-management/verification-rule-v2`, `/design-system` returned 200.
- Visual screenshot verification is still pending manual check due browser automation timeout.
