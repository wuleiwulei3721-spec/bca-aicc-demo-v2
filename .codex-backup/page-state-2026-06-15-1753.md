# Page State - 2026-06-15 17:53 +08:00

## Routes

- `/`: HTTP 200.
- `/call-management/verification-rule-v2`: HTTP 200.
- `/design-system`: HTTP 200.

## Verification Rule V2

- Default data:
  - Paylater now uses `Max Wrong = 3`.
  - Personal Banker, Layanan Cabang, KPR, Merchant Solution remain `No Limit`.
- Edit/Add modal:
  - `Max Wrong` number input width matches `Correct`.
  - `Agent Hint` has more horizontal room.
- Preview:
  - Uses a stable key tied to current rule and scenario configuration.
  - Customer Segment and Skill stay aligned with the current rule.
- Customer Verification:
  - Rule bar displays `Need N correct`, requirement chips, optional `Wrong 0/N`, and optional Agent Hint.
  - The persistent status label and info button are removed.

## Validation State

- TypeScript, lint, build, sensitive scan, residual UI scan, and HTTP smoke checks passed.
