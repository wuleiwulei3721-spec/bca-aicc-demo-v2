# Page State - 2026-05-29 16:27 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## Workspace Tabs

- `Home`: unchanged.
- `BankApp Demo`: unchanged and closable.
- `WhatsApp Demo`: unchanged and closable.
- `Live Chat`: official fixed tab, key `live-chat`, not closable, now renders `LiveChat2Page`.
- Standalone `livechat2`: no longer rendered as a separate tab.
- PSTN / Voice / Video call tabs: unchanged dynamic `call-n` tabs.

## Live Chat Replacement

- The visible tab label is `Live Chat`.
- The tab displays the longest active service duration from livechat2 timings.
- The tab flashes when a new mapped livechat2 service session is added.
- The tab shows total unread count in the upper-right badge, capped as `99+`.
- Empty state says `No Live Chat customers` and points users to WhatsApp or BankApp Demo handoff.

## Side Menu

- `Channel Simulation` contains PSTN, BankApp, and WhatsApp.
- Temporary `livechat2` menu entry has been removed.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with the existing Vite/Rolldown chunk size warning.
- `git diff --check`: passed with CRLF warnings only.
- Browser `/`: Sign In works, the official `Live Chat` tab opens and renders the new Live Chat workspace, and no visible `livechat2` text appears.
- Browser `/design-system`: loaded normally.
