# Page State - 2026-06-16 16:46 +08:00

## Inbound Workspace

- `/` still renders `BasicLayout` and `AgentWorkspace`.
- Active call toolbar now shows a compact two-line call context in the left identification block.
- The rest of the toolbar remains a single-line control bar.
- Incoming, Talking, Hold, and Mute show the call context; Idle and ended calls hide it.

## Header

- Toolbar max width no longer expands to the previous wide `72vw / 960px` value.
- Right-side notification, internal chat, divider, and agent profile controls should have more horizontal clearance.

## Validation State

- `npx tsc --noEmit --pretty false` passed.
- `npm run lint` passed.
- `npm run build` passed with the existing Vite/Rolldown chunk-size warning.
- HTTP smoke for `/` and `/design-system` returned 200.
- Screenshot/DOM verification is still pending; the attempted headless Edge CDP check was blocked by a Windows permission error.
