# Current TODO - 2026-06-12 15:27 +08:00

## P0

- Manually verify Special Rules add/delete flow in `/call-management/verification-rule-v2`.
- Verify that deleting and re-adding a special rule preserves previously configured values.
- Verify `Failure Action = Block continuation` with `Max Wrong` still blocks continuation in the verification popup.

## P1

- Consider moving special rule question selection to the same searchable checkbox picker used by base question groups if the current multi-select becomes too dense.
- Confirm whether customer-facing wording should use `Case Type Layering` or a simpler label such as `Additional Verification`.

## P2

- Add UI smoke coverage for adding/removing special rule blocks and verifying that effective questions change in the popup.
