# Page State - 2026-05-25 17:12 +08:00

## 路由

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> 重定向到 `/`

## Inbound 弹屏状态

- `Ticketing History` 仍点击打开 CRM 动态业务 tab；本轮只修正编号和日期同一行右对齐。
- `CrmPanel` 仍包含固定 `CRM` tab、可选 `Conversation` tab 和动态业务 tabs；tab key、关闭和去重逻辑未改。
- CRM tabs nav 仍保持单行固定高度；overflow 更多按钮现在外层和内层都被压缩为紧凑方形，图标居中。

## 验证记录

- `npm run lint`：通过。
- `npm run build`：通过。
- Browser `/`：主路由正常加载。
- Browser `/design-system`：页面正常加载。

## 已知限制

- 本轮不改变 PSTN、BankApp Voice、BankApp Video、Live Chat 或 Ready-aware handoff 行为。
- 仍建议用客户目标分辨率人工确认 CRM tab overflow 视觉。
