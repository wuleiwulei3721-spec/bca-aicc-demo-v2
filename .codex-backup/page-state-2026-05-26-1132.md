# Page State - 2026-05-26 11:32 +08:00

## 路由

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> 重定向到 `/`

## 话务条弹框状态

- Transfer 弹框仍保留 `Transfer Agent`、`Transfer Skill`、`Transfer Number` 三个 tab。
- `Transfer Number` tab 不再显示 `Cancel`，只显示 `Transfer` 和 `Conference`。
- Outbound Call 弹框仍保留 `Call Number` 和 `Call Agent` 两个 tab。
- `Call Number` tab 为单行 `Phone Number` 输入框 + `Call` 按钮，不再显示底部 footer 或 `Cancel`。

## Internal Chat 状态

- Header 的 Internal Chat 入口仍打开 `InternalChatModal`。
- 弹框仍为左侧 Agent Sessions + 右侧 conversation + composer 的结构。
- 当前视觉为白灰主导、轻边界、清晰气泡；减少淡蓝大背景和多层内框。

## 验证记录

- `npm run lint`：通过。
- `npm run build`：通过。
- Browser `/`：Transfer Number、Outbound Call Number、Internal Chat 均完成 smoke/visual check。
- Browser `/design-system`：正常加载。

## 已知限制

- 本轮不改话务状态机、store、mock 数据、路由或 Inbound 三栏布局。
- 仍建议在客户目标演示分辨率下人工复查 Internal Chat 视觉。
