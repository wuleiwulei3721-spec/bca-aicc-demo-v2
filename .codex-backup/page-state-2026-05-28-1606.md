# Page State - 2026-05-28 16:06 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## livechat2

- 入口：`Channel Simulation > livechat2`，应位于 WhatsApp 下方。
- Workspace tab：`livechat2`，独立于旧 `live-chat` tab key。
- 客户列表：
  - 支持收起/展开。
  - 支持 ALL / WhatsApp / BankApp / Webchat 渠道筛选。
  - Current / History 为居中文字 tab，排序按钮仍在 tab 行右侧并在 hover/focus 时显示。
  - 客户行不再显示转接图标。
  - 星标 hover 可打开颜色面板；灰色星标默认弱化。
  - 收起态保留 SLA 左侧色条，头像内显示只读星标。
  - 顶部 `ALL` 渠道标识文字使用 `var(--aicc-primary-strong)`。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，保留既有 chunk size warning。
- Browser `/`：可加载 Home tab。
- Browser `/design-system`：可加载 Design System。
- Browser livechat2 菜单点击：受当前 in-app browser 可见节点限制，未自动完成，需人工复查。
