# Page State - 2026-05-24 14:12 +08:00

## Routes

- `/`: `BasicLayout` with `AgentWorkspace`.
- `/design-system`: design system page.

## BankApp Video

- Flow remains:
  `Choose Channel -> Select Business -> Confirm Business -> Calling Agent -> Connected -> Agent Workspace -> Select Sharing Program -> View Agent Screen Sharing -> Service Closed`.
- Step 7 and step 8 are tagged `Netinfo`.
- Step 8 uses direct attachment asset `/screenshots/bankapp/video-screen-sharing.png`.

## OpenEye

- Base call screenshot remains `/screenshots/openeye-video-call.png`.
- `bankapp-video` shows the English `Desktop Share` button above hang up with translucent dark styling.
- Clicking it loads direct attachment asset `/screenshots/openeye-share-selection.png`.
- The `确定` hotspot confirms and returns to `BankApp Demo`.

## Verification

- `npm run lint`: passed.
- `npm run build`: passed, with existing chunk size warning.
- Browser `/`: verified `Desktop Share`, OpenEye selection asset, BankApp sharing asset, and both Netinfo steps.
- Browser `/design-system`: loaded successfully.
