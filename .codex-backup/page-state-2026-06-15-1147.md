# Page State - 2026-06-15 11:47 +08:00

## Routes

- `/`: Agent workspace, Customer Information `Verify` opens the V2 Customer Verification popup.
- `/call-management/verification-rules`: old V1 page remains unchanged.
- `/call-management/verification-rule-v2`: active V2 rule configuration page.
- `/design-system`: admin management examples remain available.

## Verification Rule V2 Page

- Uses standard admin page/table/modal structure.
- Toolbar actions remain `Question Bank` and `Add`.
- List columns include Channel, Skill Queue, Customer Segment, Correct Required, Max Wrong, Failure Action, Status, Updated By, Updated Time, Actions.
- Edit/Add modal:
  - Outer fields: Channel, Skill Queue, Customer Segment, Correct Required, Max Wrong, Failure Action, Status.
  - `Question Configuration` uses `Verification Scenario` selector.
  - Supports Add Scenario, Copy Scenario, Delete non-default scenario, Organization Override, Add Block.
  - Each block has Block Name, Questions picker, Correct, delete, and ordered question list.
  - No customer-visible Append/Replace mode.

## Customer Verification Popup

- Channel is readonly text.
- Skill Queue and Customer Segment are editable demo controls.
- `Scenario` options come from the matched rule scenarios.
- Switching scenario recomputes questions and requirements while retaining answer state for unchanged question ids.
- Requirement progress is block-based.
- Correct/Wrong/Skip remain icon-only actions.

## Data

- `Layanan Cabang` has explicit `Default` and `Branch to Other Services` scenarios.
- Other rules can still be generated from legacy groups/special scenarios by utility conversion.
- Question Bank still stores only question names.

## Validation State

- TypeScript no-emit passed.
- Lint passed.
- Build passed with existing chunk-size warning.
- Sensitive token scan under `src/` returned no results.
- HTTP route checks passed.
