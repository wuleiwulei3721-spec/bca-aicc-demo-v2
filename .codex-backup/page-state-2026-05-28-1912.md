# Page State - 2026-05-28 19:12 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## Transfer Modal

- Conversation 入口打开 Transfer 弹框后，`Transfer Agent` 表格行内主按钮为 `Transfer` / `Conference`。
- 更多菜单仍保留 `Force Transfer` / `Force Conference`。
- 通话工具条 Transfer 弹框原有 `Consult` / `Transfer` / `Conference` 不受影响。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，仍保留既有 chunk size warning。
