# Page State - 2026-05-24 01:24 +08:00

## BankApp Demo

- Entry: `Channel Simulation > BankApp`.
- Layout: unified `Customer Access Demo` canvas with a cross-column title bar, phone preview on the left, and AICC Process on the right.
- Phone and process sections no longer have separate card borders or backgrounds; they are divided by a subtle vertical rule inside the shared canvas.
- Right controls: clickable `Channel`, `Customer Type`, `Next Step` / `Reset`.
- Channel control: Voice / Video / Chat, clickable, resets to `Choose Channel`.
- Process rail: progressive rendering, neutral numbered markers, subtle gray arrows, active step emphasized by border/background rather than blue/green.
- End state: final step shows disabled `Completed`; Reset remains available.
- Badge policy remains unchanged: BankApp bank-owned steps show `BANK1`; process/agent-owned steps show `Netinfo`.

## WhatsApp Demo

- Entry: `Channel Simulation > WhatsApp`.
- Layout: same unified `Customer Access Demo` canvas as BankApp; WhatsApp keeps the taller screenshot aspect ratio.
- Right controls: read-only `Channel: WhatsApp`, `Next Step`, `Reset`; no `Customer Type`.
- Flow: `Request Human Agent -> Business Selection -> Queue & Agent Chat -> View Agent Workspace -> Satisfaction Rating`.
- Badge policy: `View Agent Workspace` shows `Netinfo`; all other WhatsApp steps show `Bank1`.
- Process rail: progressive rendering, neutral numbered markers, subtle gray arrows.
- End state: `Satisfaction Rating` shows disabled `Completed`; Reset remains available.

## Validation State

- Local `npm run lint`: passed.
- Local `npm run build`: passed with existing chunk size warning.
- Browser `/`: BankApp unified canvas, controls, and selected states passed.
- Browser `/`: WhatsApp unified canvas, Live Chat handoff, return preservation, and Completed state passed.
- Browser `/design-system`: passed.
