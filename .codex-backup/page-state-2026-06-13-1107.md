# Page State - 2026-06-13 11:07 +08:00

## Routes

- `/` uses Customer Information `Verify` entry to open the V2 customer verification popup.
- `/call-management/verification-rule-v2` is the active V2 rule management page.
- `/call-management/verification-rules` remains the older V1 rule page and is not removed.
- `/design-system` includes the Admin Management Page sample from the prior admin component migration.

## Verification Rule V2 Page

- Filter fields remain `Channel`, `Skill Queue`, `Customer Segment`, `Status`.
- Toolbar actions remain `Question Bank` and `Add`.
- Table columns include `Channel`, `Skill Queue`, `Customer Segment`, `Correct Required`, `Max Wrong`, `Failure Action`, `Status`, `Updated By`, `Updated Time`, `Actions`.
- `Actions` remains fixed on the right through the shared Admin table behavior.
- Add/Edit question selection should no longer crash because `Space` is imported.

## Data State

- V2 rules use `updatedAt` and optional `updatedBy`.
- New empty V2 rules default `updatedBy` to `Admin`.
- BankApp channel demo code is `BANKAPP` in routing and V2 rule matching.
- Question bank and visible rule text use Bank wording where old customer brand terms appeared before.

## Validation State

- Source sensitive-word scan under `src/` passed.
- Recently touched file mojibake scan passed.
- `npm run lint` passed.
- `npm run build` passed with existing chunk warning only.
