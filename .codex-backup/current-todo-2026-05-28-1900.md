# Current TODO - 2026-05-28 19:00 +08:00

## P0

- 人工复查 livechat2 active 会话：Header 服务时长应从 mock `accessDuration` 起算，不再从 `00:00` 开始。
- 人工复查 livechat2 active 会话：未回复计时显示为图标 + 时间，且不超过服务时长。
- 人工复查 `livechat2-005`：当前 ended 状态服务时长应为 `02:53`。
- 人工复查 `livechat2-005`：Header 不显示 `This user has ended the session.`，对话系统消息保留该文案。
- 人工复查 ended 会话：头像灰掉，右侧只有 `Close`，点击后进入 History。

## P1

- 如后续觉得 ended 原因不够明显，可考虑 header 使用极短状态 badge，但不恢复完整句子重复展示。

## P2

- 后续如接真实消息网关，服务时长和未回复时长应改由后端事件时间驱动，而不是 mock 初始化秒数。
