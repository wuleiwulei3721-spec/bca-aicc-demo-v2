# Current TODO - 2026-06-05 15:56 +08:00

## P0

- Manually review customer AUX flow:
  - Signed-out avatar menu shows only `Sign In`.
  - Signed-in avatar menu shows `AUX` and `Sign Out`.
  - Clicking `AUX` opens `Select AUX Reason`.
  - Modal lists only `Ibadah` and `Makan`.
  - `Ibadah` is selected by default.
  - `Training` and `Extension 1-7` do not appear.
  - `Cancel` keeps current status unchanged.
  - `Confirm` changes status/timer label to `AUX - Ibadah` or selected reason.
- Manually review customer safety:
  - Left menu does not show `Call Management`.
  - Left menu does not show `Routing Config`.
  - `/call-management/*` redirects to `/`.
  - `/routing-config/*` redirects to `/`.

## P1

- Decide whether this AUX branch should be pushed for a customer preview after local review.
- If publishing, verify Vercel customer access settings before sending a URL.

## P2

- If busy reasons need to be configurable in the customer release, design a safe backend/config source that does not expose unfinished admin menus.
