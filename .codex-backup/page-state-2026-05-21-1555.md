# Page State - 2026-05-21 15:55 +08:00

## 路由

- `/`：主工作台，`BasicLayout` 包裹 `AgentWorkspace`。
- `/design-system`：设计系统展示页，`BasicLayout` 包裹 `DesignSystem`。
- `*`：重定向到 `/`。

## 左侧菜单状态

- 默认收起，收起宽度为 `48px`。
- 展开态顶部为 Collapse 按钮 + `Search menu` 输入框。
- 收起态顶部只显示 Expand 按钮，不显示搜索框。
- 收起态一级菜单只显示图标，图标居中。
- `Call Management` 电话图标已水平翻转。
- 收起态二级菜单浮层通过鼠标悬浮显示。
- 点击一级或二级菜单后，当前浮层会关闭；鼠标离开当前菜单/浮层区域后可再次悬浮打开。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，保留既有 Vite chunk size warning。
- `/`：已通过 Browser smoke check确认页面加载、默认收起和 `Call Management` 菜单项存在。
