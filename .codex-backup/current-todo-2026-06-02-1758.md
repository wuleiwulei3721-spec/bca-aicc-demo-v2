# Current TODO - 2026-06-02 17:58 +08:00

## P0

- Manually review `Call Management > Routing Configuration` at the target demo resolution.
- Verify `Route Elements` defaults: VDN, Channel, Media Type, Access Site, Business Type, and Language enabled; Country, Access Account, and Access Entry disabled.
- Verify `Channels & Media` clearly communicates that channel-media relationships are stored in `channel_media`, not as a channel table multi-select.
- Verify `Site Access Volume` shows site ratios totaling 100% for each channel + media group.
- Verify `Skill Routing Rules` batch add flow: factor multi-selects, duplicate summary, overwrite checkbox, apply feedback, and published routing index.
- Verify expanded and collapsed `Call Management` sidebar flyout can open both `Routing Configuration` and `Text Channel Settings`.

## P1

- Decide whether the final demo should add create/edit modals for each routing master data table or keep the current table-driven architecture overview.
- Decide whether `Access Account` and `Access Entry` should become enabled route factors for this project or remain disabled future dimensions.
- Decide whether route rule old-version replacement should be represented visually in the demo instead of updating existing matching rows in place.

## P2

- Add browser automation tests for configuration routes once a stable browser test runner is available.
- If this becomes a backend feature, add API contracts for preview, publish, audit history, and route rule index generation.
