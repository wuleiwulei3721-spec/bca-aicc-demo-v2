# Page State - 2026-06-05 16:24 +08:00

## Production Customer App

- URL: `https://netinfo-aicc-demo-v2.vercel.app/`.
- Visible customer navigation:
  - Channel Simulation.
  - Agent Center.
  - Operations.
  - Reports.
- Hidden customer navigation:
  - Call Management.
  - Routing Config.
- Blocked routes:
  - `/call-management/busy-reasons` redirects to `/`.
  - `/routing-config/vdn` redirects to `/`.

## Production AUX Modal

- Signed-in menu:
  - `AUX`.
  - `Sign Out`.
- Removed direct menu items:
  - `AUX - Ibadah`.
  - `AUX - Makan`.
- Modal title:
  - `Select AUX Reason`.
- Enabled reason list:
  - `Ibadah`.
  - `Makan`.
- Hidden disabled reason:
  - `Training`.
- Confirm result:
  - Status label changes to `AUX - Ibadah` when default reason is confirmed.

## Local Routing Config - VDN

- Route: `/routing-config/vdn`.
- Filters:
  - `Keyword`.
- Table columns:
  - `VDN ID`.
  - `VDN Name`.
  - `Platform VDN ID`.
  - `Description`.
  - `Actions`.
- Add/Edit/View modal fields:
  - `VDN ID`.
  - `VDN Name`.
  - `Platform VDN ID`.
  - `Description`.
- Not shown:
  - `Status`.

## Local Routing Config - Access Sites

- Route: `/routing-config/sites`.
- Filters:
  - `Keyword`.
- Table columns:
  - `Site ID`.
  - `Site Name`.
  - `Owner`.
  - `Owner Phone`.
  - `Address`.
  - `Actions`.
- Add/Edit/View modal fields:
  - `Site ID`.
  - `Site Name`.
  - `Address`.
  - `Owner Name`.
  - `Owner Phone`.
- Not shown:
  - `Status`.
  - `Country`.
  - `Country Code`.

## Verification State

- `npm run lint` passed on the customer AUX branch before publish.
- `npm run build` passed on the customer AUX branch before publish.
- `git diff --check` passed before AUX commit.
- `npm run lint` passed on `codex/routing-vdn-sites-remove-status`.
- `npm run build` passed on `codex/routing-vdn-sites-remove-status`.
- Browser checked Production customer app, Production AUX modal, local VDN, local Access Sites, local Skill Routing Rules, and local Site Access Volume.
