# Page State - 2026-05-25 15:34 +08:00

## 路由

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> 重定向到 `/`

## Inbound 弹屏状态

- 左栏 `Ticketing History` 仍点击打开 CRM 动态业务 tab；本轮只调整行内编号和日期右对齐。
- 中部 `CrmPanel` 仍包含固定 `CRM` tab、可选 `Conversation` tab 和动态业务 tabs。
- 动态业务 tabs 仍由 `InteractionWorkspace` 的本地 state 管理，tab key、关闭和去重逻辑未改。
- CRM tab label 现在统一使用固定图标尺寸、文字 ellipsis 和 4px 图文间距。
- CRM tabs nav 已锁定单行固定高度，overflow 更多按钮为紧凑图标按钮。

## 验证记录

- `npm run lint`：通过。
- `npm run build`：通过。
- Browser `/`：主路由加载，Sign In 后固定 `Live Chat` tab 可见。
- Browser `/design-system`：页面正常加载。

## 已知限制

- 本轮未新增自动化视觉测试，仍需浏览器人工确认多动态 tab 场景。
- 受 in-app browser 当前可见导航区域限制，本轮未完整点击 PSTN 后逐项打开 CRM 动态 tab。
- 本轮不改变 PSTN、BankApp Voice、BankApp Video、Live Chat 或 Ready-aware handoff 行为。
