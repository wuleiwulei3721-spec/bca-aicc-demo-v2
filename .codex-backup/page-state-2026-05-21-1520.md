# Page State - 2026-05-21 15:20 +08:00

## 路由

- `/`：主工作台，`BasicLayout` 包裹 `AgentWorkspace`。
- `/design-system`：设计系统展示页，`BasicLayout` 包裹 `DesignSystem`。
- `*`：重定向到 `/`。

## 全局布局状态

- `BasicLayout` 顶部 Header、Agent Toolbar、Agent Profile、Notifications、Internal Chat 入口保持原逻辑。
- 左侧系统菜单默认收起，收起宽度为 `48px`。
- 展开宽度继续使用 `--aicc-layout-sider-width`，当前为 `220px`。
- 菜单顶部按钮控制收起/展开，按钮只有图标，带 `aria-label` 和 `title`。

## 左侧菜单状态

- 展开态一级菜单显示图标和文字。
- 展开态点击“测试菜单”“个人中心”“运营管理”会在下方展开二级菜单。
- 二级菜单仅显示文字，不配置图标。
- 收起态仅显示一级菜单图标，图标水平居中。
- 收起态含二级菜单的一级项支持右侧浮层选择二级菜单。
- “呼叫管理”“报表”当前只有一级菜单。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，保留既有 Vite chunk size warning。
- `/`：已通过 Browser 检查默认收起、展开按钮、展开态二级菜单。
- `/design-system`：已通过 Browser 检查页面可访问，侧栏可展开。
- 收起态浮层：自动化侧通过 focus 路径确认可见；CSS 同时实现 hover 触发。
