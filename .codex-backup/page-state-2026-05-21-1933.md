# Page State - 2026-05-21 19:33 +08:00

## Workspace Tabs

- Home：默认常驻 tab，标签宽度随内容适配，无关闭按钮占位。
- Inbound：点击 `Channel Simulation > PSTN / Voice` 且坐席 Ready/Idle 时打开，可关闭。
- Video Call：点击 `Channel Simulation > Video Call` 且坐席 Ready/Idle 时打开，可关闭。

## Video Call 行为

- Incoming：Video Call tab 打开，Answer 可用，OpenEye 截图不显示。
- Talking/Hold/Mute：OpenEye 截图显示为 fixed 高层级独立浮窗，可拖动。
- Hang Up：OpenEye 截图隐藏，通话进入既有 ACW/Not Ready/Ready 流程。
- 关闭 Video Call tab：OpenEye 截图隐藏，但不自动 Hang Up。

## Haloapps Call Flow

- Customer Information 的 Haloapps 渠道按钮仍可打开 `Call Flow Detail`。
- Haloapps 视频渠道不显示 `IVR Journey`。
- 非视频电话渠道仍保留 IVR Journey。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，保留既有 Vite chunk size warning。
- `/` Browser smoke check：通过 Video Call Incoming/Answer/Hang Up、Haloapps 隐藏 IVR Journey。
- `/design-system` Browser smoke check：通过基础加载。
