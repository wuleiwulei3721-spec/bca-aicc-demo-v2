# Current TODO - 2026-06-04 16:48 +08:00

## P0

- Manually save a new Voice and Video rule plan in a real browser, because the Browser plugin could not reliably type into text inputs in this session.
- Manually verify variable insertion in the access success welcome textarea at a middle cursor position.

## P1

- Continue confirming whether the simplified Voice / Video access-only scope is enough before adding queue, waiting room, callback, reconnect, or SLA rules.
- Continue planned `Channels` simplification after the rule plan modal is accepted: Channels should primarily bind Channel + Media Type to a rule plan.
- After Chinese confirmation, convert Media Service Rule Plans modal labels, validation, buttons, and default messages back to English.

## P2

- Consider replacing remaining generic CRUD `InputNumber addonAfter` usage later if AntD deprecation warnings become distracting for demos.
- Consider splitting `MediaServiceRulePlan` into media-specific nested config types later if Voice / Video configuration grows beyond access-only.
