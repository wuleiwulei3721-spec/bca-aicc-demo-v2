# Page State - 2026-06-02 20:04 +08:00

## Routes

- `/routing-config/route-elements`: ordinary CRUD page using shared admin toolbar and shared CRUD modal.
- `/routing-config/vdn`: ordinary CRUD page using shared admin toolbar and shared CRUD modal.
- `/routing-config/skill-routing-rules`: independent rules page; top-left title only.
- `/call-management/routing-configuration`: legacy route redirects to `/routing-config/route-elements`.

## Route Elements Page

- Title: `Route Element Configuration`.
- Filters: `Element ID`, `Element Name`, `Status`.
- Actions: `Search`, `Reset`, right-aligned `Add`.
- Table columns: `Element ID`, `Element Name`, `Status`, `Actions`.
- Modal fields: `Element ID`, `Element Name`, `Status`.
- Status display:
  - list/detail: compact `Enabled` / `Disabled` badge;
  - add/edit: compact switch with status text.
- Modal footer buttons now use the same 82px x 32px admin button sizing as toolbar `Search` / `Reset`.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with existing chunk size warning.
- Browser verified `/routing-config/route-elements` Add modal opens and footer actions appear.

