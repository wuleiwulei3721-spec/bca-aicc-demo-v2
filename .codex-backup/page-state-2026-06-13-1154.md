# Page State - 2026-06-13 11:54 +08:00

## Routing Config > Skill Routing Rules

- 查询字段仍为启用 Route Factors、Target Skill Queue、Status。
- 桌面目标布局：
  - 第一行：Access Site / Channel / Media Type / Language Type / Business Type / Target Skill Queue。
  - 第二行左侧：Status / Search / Reset。
  - 第二行右侧：Batch Add。
- Search 使用 primary，Reset 使用 secondary；二者仍为查询动作按钮。
- Batch Add 使用右侧主操作区 primary，自然宽度，不截断文本。
- 表格列、分页、Actions、Batch Add 弹框逻辑均未修改。

## 回归页面

- Blacklist Management：Add / Batch Add 为右侧 primary，Delete danger。
- Priority List Management：Add / Batch Add 为右侧 primary，Delete danger。
- `/design-system`：Admin Management Page 示例同步展示查询动作区和右侧主操作区的规范口径。

## 验证状态

- `npm run lint` 通过。
- `npm run build` 通过；仍只有既有 Vite/Rolldown chunk size warning。
- `git diff --check` 通过；仅有 Windows 换行提示。
- Chrome CDP 1366x768 检查通过：Skill Routing Rules 第一行 6 个查询条件，第二行左侧 Status/Search/Reset，右侧 Batch Add 与按钮行底部对齐；Blacklist/Priority Add/Batch Add 为 primary，Delete 为 danger，表格无默认横向溢出。
