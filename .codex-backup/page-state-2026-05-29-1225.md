# Page State - 2026-05-29 12:25 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## livechat2 Composer

- Composer textarea remains at the bottom of the Conversation.
- Typing `/` opens `.livechat2-quick-replies` above the full composer.
- Quick replies default to the first candidate.
- `ArrowDown` / `ArrowUp` cycles through candidates.
- `Enter` inserts the selected quick reply while the popup is open.
- Inserted quick reply keeps textarea focus and places the caret at the end of the inserted phrase.
- Without quick replies open, `Enter` still sends the message.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with the existing Vite/Rolldown chunk size warning.
- Browser `/` and `/design-system`: loaded successfully.
- livechat2 slash keyboard flow: still needs manual review because the in-app browser DOM did not expose the `livechat2` menu entry.
