# Current TODO - 2026-06-05 19:21 +08:00

## P0

- Manually review Customer Information in the target demo resolution:
  - PSTN and BankApp Voice show the second-line `Last IVR Menu` hint.
  - The existing first row channel / access duration / verification / `Verify` remains readable.
  - Long final IVR menu text truncates cleanly without overlapping nearby cards.
  - Channel tag click still opens full `Call Flow Detail`.
- Manually review identity refresh flow:
  - `Paste` fills `00000078987`.
  - Wrong ID keeps the popover open and shows inline error.
  - `Confirm` with `00000078987` refreshes Customer Information, Journey, and Ticketing History.

## P1

- Decide whether this branch should be pushed for customer preview after local review.

## P2

- If customer identity refresh must use a real CRM service later, replace `lookupCustomerIdentityRefresh()` with a service layer.
- If IVR Journey must vary by call instance later, add an interaction-level IVR path instead of using global `callFlowDetail`.
