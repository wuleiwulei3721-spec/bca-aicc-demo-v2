# Page State - 2026-05-28 15:48 +08:00

## livechat2 Customer List

- Header 保留渠道筛选和收起按钮。
- 展开态 Current / History 为居中图标-only tab，分别使用消息图标和历史图标，并保留计数 badge。
- 排序入口位于 tab 行右侧，默认隐藏，hover/focus tab 行时显示。
- 客户卡片为两行 grid：第一行客户名 + 未回复计时，第二行最新消息 + 转接/星标/Close 工具。
- 星标下拉改为 hover 触发，展开态不显示下拉箭头。
- 灰色未关注星标默认隐藏，客户行 hover/focus 时显示空心灰星。
- 红/蓝/黄星标保持常显。
- 收起态头像内星标不再有白色圆形背景，仅保留星标本体和轻量白色描边阴影。
- warning / breach 客户在展开态和收起态均继续使用左侧色条提示。

## Verification

- `npm run lint`：通过。
- `npm run build`：通过，仍保留既有 Vite/Rolldown chunk size warning；本轮额外出现插件耗时提示，不影响构建结果。
- Browser `/design-system`：页面可加载，标题为 `BANK 1 AICC Demo`。
- Browser `/`：页面可加载；自动化可数到侧栏和 `livechat2` 按钮，但当前 in-app browser 仍报告侧栏按钮无可点击 bounding box，未完成菜单点击验证。
- 待在浏览器中人工验证 livechat2 入口、展开态和收起态。
