# Page State - 2026-06-13 17:32 +08:00

## Verification Rule V2 Admin

- V2 Channel configuration options are limited to `PHONE` and `BANKAPP`.
- Customer Segment select options are ordered as Regular, Priority, Solitaire, Organization/Business.
- `Question & Special Configuration` replaces separate question and special rule add areas.
- Base groups use direct add buttons; already added groups no longer appear as add buttons.
- Special scenarios and organization override still render as configurable blocks below the add buttons.

## Order Field

- `Order` stores whether questions should be asked in configured sequence.
- Current popup behavior uses it as guidance and still allows agents to mark any visible question.
- Pass/fail calculation remains based on configured counts and answer results.

## Validation State

- Lint passed.
- Build passed with the existing chunk size warning.
- Sensitive-word scan under `src/` passed.
- HTTP smoke checks passed for the main workspace, V2 admin page, and design system.
