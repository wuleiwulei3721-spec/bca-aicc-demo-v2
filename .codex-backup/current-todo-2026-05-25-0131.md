# Current TODO - 2026-05-25 01:31 +08:00

## P0

- Push `codex/live-chat-timing-visual-cleanup`, merge to `main`, tag `v0.5.6`, and push the tag after final review.
- Reconfirm Vercel production deployment picks up `main` after merge/tag.
- During customer demo rehearsal, verify workspace tab label spacing, Live Chat `00:00` runtime start, Customer Information `Channel · access duration`, and amber SLA warning color at the target presentation resolution.

## P1

- Plan `codex/multi-inbound-interaction-tabs` for `v0.6.0`: Hang Up should stop the old phone/voice/video tab timer but keep the page for post-service registration, and a new inbound call should create a new interaction tab instead of overwriting the old one.
- If customer requires channel-specific SLA thresholds, move Live Chat warning/breach seconds from shared constants into mock/config.
- If customer asks for Webchat access, add a Webchat customer-side entry flow before re-enabling Webchat mock session visibility and filtering.

## P2

- Consider adding Playwright smoke tests for Sign In, PSTN tab duration, BankApp/WhatsApp Live Chat runtime timing, End Service cleanup, and `/design-system`.
- Consider adding a design-system example for `WorkspaceTabLabel`, `ChannelTag` with duration, and Live Chat SLA states.
