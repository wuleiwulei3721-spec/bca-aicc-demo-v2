# Page State - 2026-05-25 17:42 +08:00

## 路由

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> 重定向到 `/`

## Inbound 弹屏状态

- `Ticketing History` 仍点击打开 CRM 动态业务 tab；本轮只修正 hover 箭头占位导致的日期右边界偏移。
- Ticketing 行默认布局为左侧 ticket 类型 + 右侧 `ticket 编号 日期` meta 区。
- Ticketing hover/focus 箭头不再参与 grid 布局，而是浮在日期右侧上方。
- CRM tabs 更多按钮保持 v0.6.6 的紧凑宽度与图标居中。

## 验证记录

- `npm run lint`：通过。
- `npm run build`：通过。
- Browser `/`：主路由正常加载。
- Browser `/design-system`：页面正常加载。

## 已知限制

- 本轮不改变 PSTN、BankApp Voice、BankApp Video、Live Chat 或 Ready-aware handoff 行为。
- 仍建议用客户目标分辨率人工确认 Ticketing 日期对齐和 hover 箭头覆盖效果。
