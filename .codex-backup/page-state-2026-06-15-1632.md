# Page State - 2026-06-15 16:32 +08:00

## Verification Rule V2

- Edit/Add modal:
  - Basic Config: Channel, Skill Queue, Customer Segment, Status.
  - Verification Scenario strip remains compact.
  - Scenario Policy appears below the scenario strip:
    - Correct Required.
    - Max Wrong.
    - Failure Action.
    - Agent Hint.
  - Question Configuration remains unchanged.

## Customer Verification Popup

- Uses current scenario `maxWrongAttempts`.
- Hides Wrong count when current scenario uses `No Limit`.
- Uses current scenario `failureAction`.
- Shows current scenario `Agent Hint` when configured.
- Does not enforce question order.

## Validation State

- `npx tsc --noEmit --pretty false` passed.
- `npm run lint` passed.
- `npm run build` passed with the existing chunk size warning.
- `rg -n -i "halo|bca" src` returned no matches.
- HTTP smoke for `/`, `/call-management/verification-rule-v2`, and `/design-system` returned 200.
- `git diff --check` passed with only Windows line-ending warnings.
- Browser click-level verification is still pending because no local browser automation tool is available in this environment.
