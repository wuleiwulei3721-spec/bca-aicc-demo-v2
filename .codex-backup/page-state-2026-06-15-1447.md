# Page State - 2026-06-15 14:47 +08:00

## Call Management Routes

- `/call-management/priority-list` renders `PriorityListManagementPage`.
- `/call-management/blacklist` remains unchanged.

## Priority List Management

- Query fields:
  - Channel multi-select.
  - Identifier.
- Channel search behavior:
  - Empty value means All Channels.
  - Selecting one or more channels filters records that contain any selected channel.
- Add / Batch Add:
  - Channel is a required multi-select.
  - Channel options are active Routing Config channel names only.
  - No `All Channels` option.
  - Identifier is required.
  - Batch Add uses semicolon-separated Identifier values.
  - Remark is optional.
- Identifier tooltip:
  - Shows examples for Phone Number, Bank ID, Social Media Account, Email Address, and Email Domain.
  - Explains exact matching and email-domain matching.
- Table fields:
  - No.
  - Channel
  - Identifier
  - Match Rule
  - Remark
  - Created Date
  - Created By
- Channel display:
  - Multiple selected channels are shown as tags in one row.
- Match Rule display:
  - `Exact Match` for normal identifiers.
  - `Email Domain Match` for `@domain.tld` identifiers.
- Delete:
  - Leading checkbox selection remains enabled.
  - Toolbar Delete remains disabled until at least one record is selected.
  - Confirmed delete removes selected page records from the current demo store.

## Verification

- TypeScript no-emit passed.
- Lint passed.
- Build passed with existing chunk-size warning.
- `/call-management/priority-list` HTTP smoke returned 200.
- `/call-management/blacklist` HTTP smoke returned 200.
- Browser visual click-through was not completed because browser initialization timed out.
