# Current TODO - 2026-06-16 11:29 +08:00

## P0

- None.

## P1

- Manually verify `Priority List Management`:
  - only `Batch Add` and `Delete` are visible in the right action area;
  - selecting `Phone` and `Email Contact`, then entering `@ojk.co.id;123@gmail.com`, creates four separate rows;
  - `Email Contact + @ojk.co.id` displays `Email Domain Match`;
  - `Phone + @ojk.co.id` displays `Exact Match`;
  - duplicate preview shows `Existing No.` matching the overall priority-list row number;
  - Search / Reset still clear selected rows.
- Manually verify `Blacklist Management` was not changed by this task.

## P2

- If future backend integration needs a stricter identifier model, consider adding `Identifier Type` or channel-source mapping at service-contract level instead of blocking demo input in the page.
