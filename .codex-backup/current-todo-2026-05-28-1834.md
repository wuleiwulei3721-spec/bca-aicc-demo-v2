# Current TODO - 2026-05-28 18:34 +08:00

## P0

- 人工复查 active livechat2 会话：顶部显示渠道图标 + 客户名 + 服务时长，不显示 intent。
- 人工复查 active livechat2 会话：右侧只显示 `Transfer` 和 `End Service`。
- 人工复查 `livechat2-005`：列表头像灰掉，Conversation 顶部显示用户主动结束提示，右侧只显示 `Close`。
- 人工复查点击 `Close` 后：`livechat2-005` 从 Current 移到 History。
- 人工复查 Message Record：入口在消息区顶部，打开/关闭记录面板正常。
- 人工复查 History：结束图标时间、最新消息满宽和收起态不回归。

## P1

- 等用户提供超时配置规则后，再实现真实 timeout 自动结束计时器与流程配置入口。
- 等 Call Management 结束语配置需求明确后，再把坐席主动结束语接入配置来源。

## P2

- 后续补充稳定浏览器自动化，覆盖 livechat2 active/ended header 和 Close 流程。
