# Page State - 2026-05-24 13:48 +08:00

## Routes

- `/`: `BasicLayout` with `AgentWorkspace`.
- `/design-system`: design system page.

## BankApp Demo

- Voice path remains unchanged and still uses the existing Voice calling/connected screenshots.
- Video path now progresses:
  `Choose Channel -> Select Business -> Confirm Business -> Calling Agent -> Connected -> Agent Workspace -> Select Sharing Program -> View Agent Screen Sharing -> Service Closed`.
- `Select Sharing Program` and `View Agent Screen Sharing` are tagged `Netinfo`.
- The old BankApp connected screen share control is removed.
- Customer-side sharing screen uses `/screenshots/bankapp/video-screen-sharing.png`.

## Video Call / OpenEye

- OpenEye base call screenshot remains `/screenshots/openeye-video-call.png`.
- For `bankapp-video`, OpenEye shows a `桌面共享` button above the hang-up area.
- Clicking `桌面共享` swaps OpenEye to `/screenshots/openeye-share-selection.png`.
- Clicking the `确定` hotspot confirms sharing and activates `BankApp Demo`.
- Standard Video Call does not show the BankApp desktop sharing entry.

## Verification

- `npm run lint`: passed.
- `npm run build`: passed, with existing chunk size warning.
- Browser `/`: BankApp Video OpenEye sharing flow completed through Service Closed and Reset.
- Browser `/design-system`: loaded successfully.
