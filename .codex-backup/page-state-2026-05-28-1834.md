# Page State - 2026-05-28 18:34 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## livechat2 Conversation

- Header left：渠道图标 + 客户名 + 服务时长。
- Header 不再显示 `intent` 业务类型。
- Active header actions：`Transfer` + `End Service`。
- Ended header actions：`Close`。
- Customer ended notice：`This user has ended the session.`
- Timeout ended notice：`This session was closed due to customer timeout.`
- Message Record 入口位于消息区顶部。

## livechat2 Mock State

- `livechat2-005` 是非历史 Haloapps ended mock，会进入 Current 列表。
- `livechat2-005` 使用 `endReason: customer`，用于演示客户主动结束。
- `requestLiveChat2Workspace` 会读取 initial ended 状态，避免该会话被初始化为 active。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，保留既有 chunk size warning。
- Browser `/`：可加载 Home tab。
- Browser `/design-system`：可加载 Design System。
- livechat2 菜单点击与具体视觉仍需人工复查。
