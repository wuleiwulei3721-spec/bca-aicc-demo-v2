# Page State - 2026-06-03 18:26 +08:00

## Routes

- `/routing-config/channels`: channel master data with media rule plan binding summary.
- `/routing-config/media-service-rule-plans`: new Text media service rule plan maintenance page.
- `/call-management/text-channel-settings`: unchanged existing page, not reused by the new design.

## Channels Page

- Query: Keyword, Media Type, Status.
- List: Channel ID, Channel Name, Media Type, Rule Plan, Max Concurrent Calls, Min Scan Interval, Status, Actions.
- Add/Edit modal:
  - Channel ID, Channel Name, Media Type, Max Concurrent Calls, Min Scan Interval, Status.
  - Media Rule Plan Binding section.
  - Text selects a Text rule plan.
  - Voice / Video show `Reserved / Not configured`.

## Media Service Rule Plans Page

- Query: Keyword, Media Type, Status.
- List: Plan ID, Plan Name, Media Type, Key Rules, Description, Updated Date, Updated By, Status, Actions.
- Modal sections:
  - Basic Info.
  - Capacity & Agent No Reply.
  - Customer Timeout.
  - Lifecycle Messages.
  - Channel-specific Rules.

## Browser Verification

- `/routing-config/channels` shows `Rule Plan`, `Standard Text Service`, and `Priority Text Service`.
- `Add Channel` modal shows `Media Rule Plan Binding` and `Reserved / Not configured`.
- `/routing-config/media-service-rule-plans` shows the new page title and two Text rule plans.
- `Add Media Service Rule Plan` modal shows all expected rule sections.
