# Page State - 2026-05-21 10:42 +08:00

## 路由

- `/`：主工作台，`BasicLayout` 包裹 `AgentWorkspace`。
- `/design-system`：设计系统展示页，`BasicLayout` 包裹 `DesignSystem`。
- `*`：重定向到 `/`。

## 全局布局

- `BasicLayout` 提供顶部 BANK 1 Header、侧栏、Agent Profile Area、Agent Toolbar、Notifications、Internal Chat 和主内容区。
- 坐席状态包含 Ready、Not Ready、AUX、Unsigned 等。
- 话务状态包含 Idle、Incoming、Talking、Hold、Mute。
- Ready + Idle 可触发模拟 Inbound 来电并打开 Inbound tab。

## Inbound 工作台

Inbound 当前为三栏：

- Left：客户信息和业务卡片。
- Center：CRM workspace。
- Right：Assistant 和 Connection 状态。

已知交互：

- Customer Journey 可打开详情 modal。
- Ticketing History 可打开 CRM 动态业务 tab。
- Next Best Action 可打开 CRM 动态业务 tab。
- Quick Action 可打开 CRM 动态业务 tab。
- Customer Verification、Call Flow Detail、Send Email 等 modal 已存在。
- 当前工作区新增了 Contact Management 相关文件，需要后续验证入口与状态。

## 资源状态

- `publiccode-based BANK 1 CRM fallback` 已存在。
- `publiccode-based BANK 1 Assistant fallback` 已存在。
- CRM 与 Assistant 区域仍保留 fallback 逻辑。

## 验证状态

- 本轮仅做文档和上下文规则修改。
- 本轮未运行 `npm run lint`。
- 本轮未运行 `npm run build`。
- 本轮未启动浏览器验证。



