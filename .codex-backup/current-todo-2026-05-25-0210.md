# Current TODO - 2026-05-25 02:10 +08:00

## P0

- Push `codex/multi-inbound-interaction-tabs`, merge to `main`, tag `v0.6.0`, and push the tag after final review.
- Reconfirm Vercel production deployment picks up `main` after merge/tag.
- During customer demo rehearsal, verify old ended PSTN / Voice Call / Video Call tabs stay available for post-service registration while new calls open new tabs.

## P1

- Confirm with the customer whether single active call plus multiple ended tabs is sufficient, or whether true simultaneous active calls are needed later.
- If true multi-active calls are required, plan a separate toolbar state model because the current toolbar intentionally controls only one active call.
- Consider adding a visual ended-state cue inside the page body if customers find frozen tab duration alone unclear.

## P2

- Consider adding Playwright smoke tests for multi call tabs, active-tab close prevention, duration freeze, and BankApp Video OpenEye binding.
- Consider persisting post-service draft content if customer expects old ended tabs to survive full page refresh.
