# Page State - 2026-06-17 18:50 +08:00

## Routes

- `/call-management/priority-list`
  - Customer priority list configuration page.
  - Batch-only maintenance entry.
  - Channel is multi-select in the Batch Add modal.
  - Identifier textarea supports semicolon-separated batch input.
  - Match Rule is user-selected and defaults to `Exact Match`.
  - Records are saved as one row per selected Channel and Identifier.
  - Duplicate detection uses `Channel + normalized Identifier + Match Rule`.
  - Delete, Search, Reset, and selection behavior remain unchanged.

- `/call-management/blacklist`
  - Smoke-tested and unchanged by this round.

## Priority List Fields

- Search:
  - Channel
  - Identifier
- List:
  - Selection checkbox
  - No.
  - Channel
  - Identifier
  - Match Rule
  - Remark
  - Created Date
  - Created By
- Batch Add:
  - Channel
  - Identifier
  - Match Rule
  - Remark

## Match Rules

- `Exact Match`: customer identifier must equal the configured value.
- `Partial Match`: customer identifier contains the configured value.
- `Email Domain Match` is removed.
- Email domains such as `@ojk.co.id` are no longer auto-classified; they use whichever rule the user selects.

## Verification

- TypeScript check passed.
- ESLint passed.
- Production build passed with existing large chunk warning.
- HTTP smoke for priority list and blacklist returned 200.
