# Page State - 2026-06-13 18:21 +08:00

## Verification Rule V2 Admin

- The edit modal has one `Max Wrong` field.
- `No Limit` checked means unlimited wrong attempts and hides the number input.
- `No Limit` unchecked shows the numeric `Max Wrong` input.
- Base question groups still use `Questions` to select questions and `Correct` for required correct count.
- Alternative keeps its compact tooltip explaining replacement behavior.
- Special scenario rows use a wider `Scenario` input and right-aligned controls.

## Default Data

- `Layanan Cabang` still has the base branch-service static questions with required correct count 2.
- Its special scenario is now one `Branch Combined Verification` item.
- `Branch Combined Verification` contains 6 questions and requires 6 correct answers.

## Validation State

- Lint passed.
- Build passed with the existing chunk size warning.
- Sensitive-word scan under `src/` passed.
- HTTP checks for `/`, `/call-management/verification-rule-v2`, and `/design-system` returned 200.
- Diff whitespace check passed with Windows line-ending warnings only.
