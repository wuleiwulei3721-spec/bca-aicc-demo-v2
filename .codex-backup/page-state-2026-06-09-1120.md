# Page State - 2026-06-09 11:20 +08:00

## Transfer Modal

- `Transfer Agent` tab shows Keyword search, Skill Queue filter, Search, and row count.
- `Transfer Agent` tab no longer shows a Status filter.
- Agent Status column remains visible and every row shows `Ready`.

## Outbound Call Modal

- `Call Agent` tab shows Keyword search, Skill Queue filter, Search, and row count.
- `Call Agent` tab no longer shows a Status filter.
- Agent Status column remains visible and every row shows `Ready`.

## Validation State

- Completed: `npm run lint`.
- Completed: `npm run build`.
- Completed: local Chrome CDP smoke checks for both affected modal tabs.
