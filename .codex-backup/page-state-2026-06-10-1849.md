# Page State - 2026-06-10 18:49 +08:00

## Routes

- `/login` remains the public login page.
- `/`, `/design-system`, and visible `/call-management/*` routes still require auth session.
- `/routing-config/*` remains hidden from the customer main line and redirects back to `/`.

## Header And Agent Status

- Unsigned profile dropdown still shows `Sign In` with `Voice only`, `Digital only`, and `Voice + Digital`.
- `Digital only` / `Voice + Digital` sign-in enters `Ready` and seeds the two default Live Chat Current customers once.
- `Voice only` sign-in enters `Ready` without seeding Live Chat customers.
- Ready / Not Ready signed-in dropdown still shows current service mode, `AUX` group, enabled busy reasons, and media `Sign Out`.
- AUX / Pre-AUX dropdown still shows current status, `Ready`, and media `Sign Out`; it does not show other AUX reasons.
- Media `Sign Out` blocks with `Active Service in Progress` when current customer work exists; otherwise it opens `Confirm Sign Out`.
- System red `Log Out` blocks with `Active Service in Progress` when current customer work exists; otherwise it opens `Confirm Log Out`.

## Live Chat State

- Default Current customers are `livechat2-001` and `livechat2-005`.
- Default customers appear only during digital-capable media sign-in.
- Closing or ending all Current customers leaves `No current Live Chat customers`.
- Returning from AUX to Ready after Current is empty does not re-create default customers.
- Channel Simulation handoff still creates new handoff sessions and is not affected by the default seed option.

## Verification State

- `npm run lint` passed.
- `npm run build` passed with the existing Vite chunk size warning.
- Headless Chrome/CDP smoke passed on temporary local port 5177 for Live Chat default seeding, Ready no-reseed behavior, active Live Chat exit blocking, no-active-service confirmations, and active PSTN exit blocking.
