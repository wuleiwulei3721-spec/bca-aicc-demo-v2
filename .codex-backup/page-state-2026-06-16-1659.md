# Page State - 2026-06-16 16:59 +08:00

## Inbound Workspace

- `/` still renders `BasicLayout` and `AgentWorkspace`.
- Active call toolbar identification block displays two rows with a shared label/value grid.
- Current PSTN target display:
  - `IVR 08123456789`
  - `SKILL Credit card activation`
- Incoming, Talking, Hold, and Mute show the call context; Idle and ended calls hide it.

## Header

- The toolbar identification block is content-adaptive rather than fixed-width.
- Toolbar controls remain single-line.
- Right-side Header controls should have more usable space than the earlier fixed-width two-line version.

## Validation State

- `npx tsc --noEmit --pretty false` passed.
- `npm run lint` passed.
- `npm run build` passed with the existing Vite/Rolldown chunk-size warning.
- HTTP smoke for `/` and `/design-system` returned 200.
- Manual screenshot/DOM verification remains pending.
