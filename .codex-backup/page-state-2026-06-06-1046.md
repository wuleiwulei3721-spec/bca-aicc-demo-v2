# Page State - 2026-06-06 10:46 +08:00

## Branch

- `codex/admin-config-latest`

## Menus And Routes

- `/` loads the main BANK 1 workspace.
- `Call Management` is visible in the left menu on the internal branch.
- `Routing Config` is visible in the left menu on the internal branch.
- `/call-management/global-control-configuration` opens Global Control Configuration.
- `/call-management/busy-reasons` opens Busy Reason Management.
- `/routing-config/channels` opens Channels.
- `/routing-config/skill-queues` opens Skill Queues.

## Current Admin UI Expectations

- Right-avatar AUX uses a single `AUX` menu item and a reason selection modal.
- Busy Reason Management edits the same reason data used by AUX.
- Skill Queues default work time label is `Default 24/7`.
- Working Time Plans does not contain a real `Default 24/7` plan record.

## Verification State

- `npm run lint` passed.
- `npm run build` passed with the existing Vite chunk size warning.
- `git diff --check` passed.
- Browser `/` confirmed the expanded left menu shows `Call Management` and `Routing Config`.
- Browser `/design-system`, `/call-management/global-control-configuration`, `/call-management/busy-reasons`, `/routing-config/channels`, `/routing-config/skill-queues`, and `/routing-config/working-time-plans` loaded successfully.
- Browser `Skill Queues` showed `Default 24/7` and did not show `Default 24x7`.
- Browser `Working Time Plans` did not show a real `Default 24/7` record.
- Browser AUX check confirmed the signed-in menu shows one `AUX` item and the modal lists enabled reasons only.
