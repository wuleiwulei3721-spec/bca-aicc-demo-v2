# Page State - 2026-05-27 22:56 +08:00

## Transfer Modal

- `Transfer Agent` 页：
  - 查询栏：Search name or employee ID、Skill Queue、Status、Search、agents count。
  - 表格列：marker、Employee ID、Department、Name、Skill Name、Status、Extension、Actions。
  - call 变体动作：Consult、Transfer、Conference。
  - conversation 变体动作：Request Transfer、Request Conference、More actions。
- `Transfer Skill` 页保持原搜索、Skill ID、Skill Name、Transfer 动作。
- `Transfer Number` 页：
  - 只保留号码输入框、Transfer、Conference。
  - 不显示 System Number。
  - 不显示 Manual Number。

## Outbound Call Modal

- `Call Number` 页保持 Phone Number 输入框 + Call 按钮单行布局。
- `Call Agent` 页：
  - 查询栏与 Transfer Agent 一致，包含 Skill Queue 和 Status。
  - 表格新增 Skill Name 和 Status，行内 Call 按钮更紧凑。

## Shared Modal Styling

- 弹框内 SearchInput / 输入框 / Select 使用 28px 控件高度。
- 弹框内行内小按钮使用 24px 高度，减少表格行高。
- placeholder、输入文字、prefix icon、suffix icon 已按 flex/line-height 居中。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过。
- `/design-system`：加载正常。
- `/`：主页面可加载，Internal Chat 弹框可打开。
- Transfer / Outbound 深层点击仍需人工最终确认。
