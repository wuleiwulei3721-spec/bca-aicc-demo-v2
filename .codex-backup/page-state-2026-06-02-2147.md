# Page State - 2026-06-02 21:47 +08:00

## Routes

- `/routing-config/vdn`: ordinary CRUD page using shared admin toolbar and shared CRUD modal.
- `/routing-config/sites`: ordinary CRUD page using shared admin toolbar and shared CRUD modal.
- `/call-management/routing-configuration`: legacy route redirects to `/routing-config/route-elements`.

## VDN Page

- Query filters: `Keyword`, `Status`.
- Keyword placeholder: `VDN ID / Name / Platform ID`.
- Keyword match fields: `vdnCode`, `vdnName`, `platformVdnId`.
- Status filter: `All / Enabled / Disabled`.
- Modal layout from previous state remains: `Platform VDN ID` required, `Description` full-width.

## Sites Page

- Query filters: `Keyword`, `Status`.
- Keyword placeholder: `Site ID / Name`.
- Keyword match fields: `siteCode`, `siteName`.
- Status filter: `All / Enabled / Disabled`.
- Table columns: `Site ID`, `Site Name`, `Owner`, `Owner Phone`, `Address`, `Status`, `Actions`.
- Add/Edit modal fields: `Site ID`, `Site Name`, `Address`, `Owner Name`, `Owner Phone`, `Status`.
- No top timezone hint.
- No visible Country/Country Code field.
- Add modal title: `Add Site`.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with existing chunk size warning.
- Browser verified VDN and Sites filter/page/modal changes.

