# Page State - 2026-05-28 16:42 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## livechat2 Tab Behavior

- `livechat2` tab duration 只读取 active livechat2 sessions。
- Duration 显示当前 active 客户中服务时间最长的一位。
- 当前没有 active livechat2 客户时，tab 不显示 duration。
- Tab 不传入 `slaState`，因此页签时间不变 warning / breach 颜色。
- 新接入 active 客户仍通过 `flashUntil` 触发 tab 短闪。

## livechat2 Customer Panel

- 收起态当前服务会话头像内可显示星标。
- 收起态历史会话头像内不显示星标。
- 其它客户列表交互延续当前实现：文字 Current / History tab、hover 排序、渠道筛选、SLA 左侧色条、灰色星标弱化。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，保留既有 chunk size warning。
- Browser `/`：可加载 Home tab。
- Browser `/design-system`：可加载 Design System。
- livechat2 菜单点击与具体 tab/收起态视觉仍需人工复查。
