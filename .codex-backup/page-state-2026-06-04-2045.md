# Page State - 2026-06-04 20:45 +08:00

## Routing

- `/routing-config/channel-types` -> `ChannelTypesPage`.
- `/routing-config/channels` -> `ChannelsPage`.
- `/routing-config/access-accounts` -> redirect to `/routing-config/channels`.
- `/routing-config/media-service-rule-plans` -> redirect to `/routing-config/channels`.
- Left menu `Routing Config` includes Channel Types and Channels, but no Access Accounts or Media Service Rule Plans.

## Channel Types

- Read-only business user page.
- Shows Type Code, Channel Type, Supported Media, Access Parameter Template, Category, License, Status.
- No Add/Delete actions.
- Phone has no access parameters.

## Channels

- List shows Channel ID, Channel Name, Channel Type, Media Type, Access Parameters, Status, Actions.
- No Add/Delete Channel toolbar action.
- Row actions:
  - Edit: Media Type, Status, technical access parameters.
  - Business Config: media-specific service behavior.
  - Accounts: multiple official accounts under the selected channel.
- Email mock:
  - `EMAIL` / Email Contact.
  - `EMAIL_PRIORITY` / Email Priority.
- Instagram mock:
  - One `INSTAGRAM` channel with `bank1.official` and `bank1.priority` accounts.

## Skill Queues

- Queue configuration now lives here:
  - Non-working Time Message.
  - Max Queue Customers.
  - Queue Waiting Message.
  - Queue Timeout.
  - Queue Timeout Message.
- Work Time Plan empty value still displays as `Default 24x7`.

## Verification Notes

- `npm run lint`: passed.
- `npm run build`: passed.
- HTTP smoke check on dev server returned 200 for Channel Types, Channels, and Skill Queues.
- Real browser modal/redirect checks remain manual because the current automation environment lacks Playwright/browser tooling.
