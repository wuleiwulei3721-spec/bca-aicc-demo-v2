# Page State - 2026-06-03 12:26 +08:00

## Skill Routing Rules

- Route: `/routing-config/skill-routing-rules`.
- Page title: `技能路由规则配置`.
- The page title appears only once; the table card has no repeated title.
- Main toolbar remains `Keyword`, `Target Skill Queue`, `Status`, `Search`, `Reset`, and right-aligned `Batch Add`.
- `Published Routing Rule Index` is no longer displayed on the page.
- Rule table still shows `Rule ID`, `Route Conditions`, `Target Skill Queue`, `Priority`, `Effective From`, `Status`, and `Actions`.

## Batch Add Modal

- Title: `Batch Add Skill Routing Rules`.
- Sections: `Route Elements`, `Target Routing`, and conditional `Duplicate Route Combinations`.
- Enabled factors are rendered one per row with full-width multi-selects.
- `Target Skill Queue` is separated from route factors.
- `Priority`, `Overwrite duplicate route combinations`, and combination summary cards are not shown.
- Duplicate combinations render in a table with row checkboxes, factor values, Original Skill Queue, and Target Skill Queue.
- Duplicate rows are selected by default; unselected duplicate rows are not overwritten.
- If no new rules are created and all duplicates are unselected, Save shows `No routing rule changes selected.` and keeps the modal open.

## Verification State

- Lint passed.
- Build passed.
- Browser smoke check passed for page title, missing index section, Batch Add sections, duplicate table, and unselected duplicate warning.
