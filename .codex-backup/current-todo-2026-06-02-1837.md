# Current TODO - 2026-06-02 18:37 +08:00

## P0

- Manually review `Routing Config` collapsed and expanded side menu behavior on the target demo screen.
- Manually review every `Routing Config` child page Add / View / Edit / Delete modal layout, especially long table pages with horizontal scroll.
- Confirm with stakeholders whether `Channel Media` should remain a separate child page or be renamed as part of `Channels`.

## P1

- Add richer validation for channel-specific `extensionConfig` if backend field contracts become available.
- Add route rule risk preview for no-agent, queue-full, out-of-hours, and inactive target queue when a real routing service contract exists.
- Decide whether rule delete should physically remove demo rows or set status to `Disabled/Replaced` in the future product spec.

## P2

- Consider localStorage persistence for demo CRUD if presenters need changes to survive refresh.
- Consider extracting routing factor value lookup helpers if more pages need advanced rule previews.
- Consider code splitting later if the Vite chunk size warning becomes a delivery concern.

## Completed This Turn

- Split old tabs page into first-level `Routing Config` with independent pages.
- Added local demo CRUD for normal config pages.
- Added independent Skill Routing Rules batch page.
- Removed timezone from Sites and kept timezone in Working Time Plans.
- Added old URL redirect.
- Ran lint, build, browser smoke, and representative interaction checks.
