# Current TODO - 2026-06-05 11:30 +08:00

## P0

- Manually review `Call Management > 示忙原因维护`:
  - Query by keyword matches ID / reason / remark.
  - Filter by default yes/no.
  - Filter by enabled/disabled status.
  - Add/View/Edit/Delete modals open and close normally.
  - Saving one reason as default automatically clears the previous default.
- Manually review header AUX:
  - Signed-out state shows Sign In only.
  - Signed-in state shows enabled busy reasons.
  - Disabled busy reasons do not appear.
  - After enabling a disabled reason in the maintenance page, AUX menu includes it without refresh.

## P1

- Decide whether deleting the current default busy reason should be blocked or allowed.
- Decide whether at least one default busy reason must always exist.
- Decide whether default busy reason should be required to stay enabled.

## P2

- If Call Management grows further, consider a shared CRUD component for these local admin pages.
