# Page State - 2026-05-22 19:52 +08:00

## BankApp Demo

- Entry: left menu `Customer Simulator > BankApp`.
- Workspace tab: `BankApp Demo`, closable.
- Page structure:
  - Customer BankApp phone simulator.
  - AICC Routing timeline.
  - Agent Desktop Outcome.
- Supported contact methods: Text Chat, Voice Call, Video Call.
- Supported business types: Mobile Banking Login, Card Issue, Transaction Dispute, Account Information.
- Screenshot assets:
  - `/screenshots/bankapp/channel-selection.png`
  - `/screenshots/bankapp/text-login.png`
  - `/screenshots/bankapp/voice-phone-number.png`
  - `/screenshots/bankapp/voice-skill-selection-en.png`
  - `/screenshots/bankapp/voice-skill-confirm-en.png`

## Agent Workspace Integration

- Text Chat `Connect to Agent` opens Live Chat and focuses Sari Amelia / BankApp.
- Voice Call `Connect to Agent` triggers the existing voice incoming state when agent status is `Ready` and call status is `Idle`.
- Voice Call Inbound customer uses `BankApp Voice`.
- Video Call `Connect to Agent` triggers the existing video incoming state when agent status is `Ready` and call status is `Idle`.
- Video Call still shows the OpenEye floating window only after the agent answers.

## Verification

- `npm run lint`: passed.
- `npm run build`: passed with existing chunk size warning.
- Browser smoke check `/`: BankApp menu entry, tab open, Text Chat focus, Voice Call trigger, Video Call trigger, OpenEye after answer, and Reset Demo passed.
- Browser smoke check `/design-system`: passed.
