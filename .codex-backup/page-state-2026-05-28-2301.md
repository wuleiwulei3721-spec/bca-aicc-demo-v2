# Page State - 2026-05-28 23:01 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## livechat2 Message Record

- 右侧 `Message Record` tab 保持在 `Assistant` / `Connection` 旁，可关闭。
- 顶部搜索区包含 From / To 日期输入、消息内容搜索框和 `Search` 按钮。
- 默认日期范围为近 7 天，搜索结果按发送时间倒序。
- 结果行显示发送人、消息内容、发送时间。
- `Locate` 按钮在 hover / focus 时显示，点击后重置搜索并定位中间消息。

## livechat2 Conversation

- 每条消息 article 带稳定 `data-livechat2-message-id`。
- 收到定位请求后滚动到对应消息，并给消息气泡添加短暂高亮动画。
- 输入区仍保留 Emoji、File、Message Record 三个图标和 Send 按钮。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，仍保留既有 plugin timings 与 Vite/Rolldown chunk size warning。
- Browser `/`：页面可加载，Home tab 正常。
- Browser `/design-system`：页面可加载，Design System 文本可见。
- Browser livechat2 点击链路：当前 in-app browser 可见 DOM 未暴露左侧 `livechat2` 菜单项，需人工复查。
