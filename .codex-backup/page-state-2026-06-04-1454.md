# Page State - 2026-06-04 14:54 +08:00

## 路由状态

- `/routing-config/media-service-rule-plans`：当前本地开发分支可访问，客户 Production `main` 仍隐藏管理路由。
- `/routing-config/channels`：当前本地开发分支可访问，Rule Plan 绑定展示正常。
- `/call-management/*` 与 `/routing-config/*` 在客户 Production 版本仍重定向到 `/`。

## Media Service Rule Plans

- 列表字段保持 Plan ID、Plan Name、Media Type、Description、Updated Date、Updated By、Status、Actions。
- Add/Edit/View 弹框分区：
  - Basic Info。
  - Customer Service Configuration。
  - Agent Service Configuration。
- Add 默认值：
  - Max Concurrent Access：50 items。
  - Minimum Scan Interval：30 sec。
  - Max Queue Customers：20 customers。
  - Queue Timeout：10 min。
  - Pre-timeout Reminder Time：1 min。
  - Customer No Reply Timeout：5 min。
  - Agent No Reply Timeout：120 sec。
  - Webchat Message Recall Limit：120 sec。
  - Warning：60 sec。
  - Breach：120 sec。
- 模板变量提示保留 `{customerName}`、`{agentName}`、`{timeoutMinutes}`、`{reminderMinutes}`、`{estimatedWaitMinutes}`、`{workTime}`。
- 旧 `Channel-specific Rules`、`Queue Alert`、`Recipients` 不再展示。

## 交互状态

- Add 弹框必填校验可阻止保存。
- View 弹框只读展示新字段。
- Edit 保存后更新列表 Updated Date / Updated By。
- 被 Channel Media Rule Binding 引用的规则方案不能删除。

## Channels 回归

- `/routing-config/channels` Rule Plan 列仍显示 `Standard Text Service` 和 `Priority Text Service`。
- 本轮未修改 Channels 表单结构。

## 验证

- `npm run lint` 通过。
- `npm run build` 通过。
- Browser smoke check 已覆盖 Media Service Rule Plans Add/View/Edit/Delete guard 和 Channels 列表回归。
