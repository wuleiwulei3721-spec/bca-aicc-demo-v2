# Current TODO - 2026-06-03 19:52 +08:00

## P0

- User visual review of Routing Config menu labels, page titles, and modal titles after English alignment.
- Manually open Add/Edit/View/Delete modals from several Routing Config pages to confirm all titles render English in the browser.

## P1

- Continue enforcing the admin page skeleton for custom Routing Config pages:
  `PageContainer > section.routing-config-page > BaseCard compact > admin-toolbar + BaseTable`.
- If new Routing Config pages are added, start from the existing standard page wrappers instead of creating page-specific toolbar/table styles.

## P2

- Add a small smoke test or static check that prevents non-English `PageContainer title` values under `src/pages/routing-config`.
- Consider splitting the large `RoutingConfigDataPages.tsx` file after routing requirements stabilize.
