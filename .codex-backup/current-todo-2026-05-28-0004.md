# Current TODO - 2026-05-28 00:04 +08:00

## P0

- 人工复查 `Transfer Modal > Transfer Number`：号码输入框、`Transfer`、`Conference` 是否与其它 tab 查询控件高度一致。
- 人工复查 `Outbound Call Modal > Call Number`：号码输入框、prefix icon、`Call` 是否与其它 tab 查询控件高度一致。
- 发布前再次执行 `git diff --name-only main..HEAD`，确认没有 `livechat2` / `LiveChat2` 相关文件。

## P1

- 如还需要继续压缩弹框密度，优先只调整 `.aicc-modal` 内部弹框作用域样式，避免影响主工作台。

## P2

- 后续可以补一条稳定的浏览器 smoke path，用于直接打开 Transfer / Outbound 弹框并截图检查控件对齐。
