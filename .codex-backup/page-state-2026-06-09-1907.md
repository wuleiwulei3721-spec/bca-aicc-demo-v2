# Page State - 2026-06-09 19:07 +08:00

## Login Page

- Route: `/login`.
- Fields: User Name, Password, EXT.
- Removed: PIN input, captcha image/button, captcha refresh behavior, and PIN validation errors.
- Demo credentials: `888888 / 888888`.
- EXT remains optional and is saved only as session metadata.

## Auth Flow

- Wrong credentials stay on `/login` and show the mock LDAP error.
- Correct `888888 / 888888` enters `/` with media status still `Unsigned`.
- Authenticated visits to `/design-system` are allowed.

## Header Exit Flow

- Media `Sign Out` is inside the agent status dropdown.
- Clicking media `Sign Out` opens `Confirm Sign Out`.
- Cancel keeps the current media mode; confirm returns to `Unsigned` and stays in the workspace.
- System `Log Out` remains the red power button at the top right.
- Clicking system `Log Out` opens `Confirm Log Out`.
- Cancel keeps the current auth session; confirm clears the auth session, resets media state, and returns to `/login`.

## Validation State

- Completed: `npm run lint`.
- Completed: `npm run build`.
- Completed: local Chrome CDP smoke for login without PIN, Sign Out confirmation, Log Out confirmation, and `/design-system`.
