# Current TODO - 2026-05-27 02:07 +08:00

## P0

- 用户最终目视确认 Modal 回调视觉：Transfer / Outbound / Internal Chat 应为浅蓝标题栏、灰蓝 body、白色内容面、内容不贴边。
- 确认 Transfer 搜索框与 Search 按钮高度一致，行内 `Consult` / `Transfer` / `Conference` 按钮尺寸统一。
- 确认 Outbound Call Number 仍为 `Phone Number` 输入框 + 右侧 `Call` 单行布局，无 `Cancel` 和旧 footer。

## P1

- 如果用户仍觉得标题栏不够系统感，可在不改结构的前提下把浅蓝标题栏加深一档，但不要回到纯白标题栏。
- 如果用户觉得 table 仍偏重，可只微调 table header 和 row border，不再改 Modal 基础结构。

## P2

- 后续可补充 Playwright smoke test，覆盖 Transfer / Outbound / Internal Chat 弹框是否存在、关键按钮是否出现以及 `Cancel` 是否回归。
