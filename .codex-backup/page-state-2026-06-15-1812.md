# Page State - 2026-06-15 18:12 +08:00

## Routes

- `/`: HTTP 200.
- `/call-management/verification-rule-v2`: HTTP 200.
- `/design-system`: HTTP 200.

## Verification Rule V2

- Max Wrong semantics corrected:
  - `maksimal 3 kali salah jawab` means `Max Wrong = 3`.
  - `tidak ada ketentuan maksimal salah jawab/salah` means `No Limit`.
- Legacy group-based rules now generate default scenarios with inherited rule-level max wrong policy.
- Legacy special scenarios inherit the base scenario max wrong policy.
- Runtime compatibility handles stale generated legacy scenarios whose max wrong was previously stored as null.

## Validation State

- TypeScript, lint, production build, sensitive scan, HTTP smoke, and `git diff --check` passed.
- `npm run build` still shows existing Vite chunk-size warning only.
- `git diff --check` still shows existing Windows LF/CRLF warnings only.
