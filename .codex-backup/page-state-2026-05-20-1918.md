# Page State - 2026-05-20 19:18 +08:00

## `/`

组件链路：

```text
App
  ConfigProvider
  RouterProvider
    BasicLayout
      AgentWorkspace
```

页面状态：

- 默认显示 Home tab。
- Home tab 显示 Agent Desktop Ready。
- Ready + Idle 状态下约 2 秒后模拟 Incoming。
- Incoming 时打开 Inbound tab。
- Inbound tab 可关闭。

关键交互：

- Answer：Incoming -> Talking。
- Auto Answer：Incoming 按配置秒数自动 -> Talking。
- Hold：Talking -> Hold，再次点击回 Talking。
- Mute：Talking -> Mute，再次点击回 Talking。
- Hang Up：回 Idle，进入 Not Ready/ACW，约 5 秒后回 Ready。
- Transfer / Outbound / Internal Chat / Settings 等弹窗已实现。

## Inbound tab

组件链路：

```text
InboundPage
  LeftColumn
  CrmPanel
  AssistantPanel
```

左栏：

- Customer Information。
- Customer Journey。
- Ticketing History。
- Next Best Action。
- Quick Action。

中栏：

- 固定 CRM tab。
- 动态业务 tabs。
- CRM screenshot path：`code-based BANK 1 CRM fallback`。
- 当前截图缺失时显示 fallback。

右栏：

- Assistant tab。
- Connection tab。
- Assistant screenshot path：`code-based BANK 1 Assistant fallback`。
- 当前截图缺失时显示 fallback。

## `/design-system`

组件链路：

```text
BasicLayout
  DesignSystem
```

页面状态：

- 展示统一 UI 规范。
- 包含颜色、字体、间距、按钮、状态、卡片、弹窗、表格、tabs、timeline、chat、toolbar 和可复用组件契约。

## 资源状态

存在：

- `public/favicon.svg`
- `public/icons.svg`

缺失：

- `publiccode-based BANK 1 CRM fallback`
- `publiccode-based BANK 1 Assistant fallback`



