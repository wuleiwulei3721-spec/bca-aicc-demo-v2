# Page State - 2026-05-28 19:26 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## Transfer Modal

- `Transfer Skill` 页签表格列：`Skill ID`、`Skill Name`、`Agents`、`Ready`、`Action`。
- `Agents` 表示签入该技能的坐席数量。
- `Ready` 表示该技能队列中状态为 Ready 的空闲坐席数量。
- 新增列数据由 `transferAgents` 统计，不单独维护在 `transferSkills` mock 中。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，仍保留既有 chunk size warning。
- Browser `/`：可加载 Home tab。
- Browser `/design-system`：可加载 Design System。
