# Page State - 2026-05-29 17:42 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## Live Chat

- Official `Live Chat` tab renders `LiveChat2Page`.
- Agent-ended sessions close directly after confirmation and move to History.
- Customer-ended and timeout-ended sessions still show `Close`.
- Repeated demo handoff creates new session instances based on livechat2 mock templates.
- Quote send uses a structured `quotedMessage` field and renders the quote as a shallow visual block.

## CRM / Assistant Tabs

- Ticket dynamic CRM tabs display CRM IDs from ticket references.
- Assistant fixed and extra tabs share a unified icon-text label layout.
- CRM and Assistant tab navs reserve room for the more button to keep final close buttons clickable.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with the existing Vite/Rolldown chunk size warning.
- Browser `/`: loaded; after Sign In, the fixed `Live Chat` tab was visible. Detailed `Live Chat` tab interaction still needs manual review because the in-app browser did not stably complete the tab switch.
- Browser `/design-system`: loaded; `UI Design System` and base sections were visible.
