# Page State - 2026-06-04 16:12 +08:00

## Routing

- Local branch `codex/text-channel-config-settings`:
  - `/routing-config/media-service-rule-plans` remains accessible.
  - `/routing-config/channels` remains accessible.
  - `Call Management` and `Routing Config` are visible in the local side menu.
- Production `main` remains the customer hidden-admin-menus version.

## Media Service Rule Plans

- List page remains unchanged.
- Add/Edit modal:
  - Temporary Chinese title, labels, validation, and buttons remain.
  - Top-level sections: `基础信息`, `客户服务配置`, `坐席服务配置`.
  - Message fields with useful variables show a right-side `插入变量` dropdown.
  - Message fields without useful variables do not show variable insertion.
  - Numeric fields use local `InputNumber + small unit text`, with units about 11px and lighter visual weight.
  - Validation rules are unchanged: required key messages, positive numbers, customer reminder before timeout, warning <= breach, breach <= agent no-reply timeout.
- View modal:
  - Read-only Chinese display.
  - No `插入变量` controls.
- Delete modal:
  - Referenced rule plans still cannot be deleted.

## Channels

- `Channels` still displays Rule Plan bindings for Text media.
- No rule plan type or binding structure changed in this round.

## Verification Notes

- `npm run lint`: passed.
- `npm run build`: passed.
- Browser checks were run on `http://127.0.0.1:5181`.
- Add modal verified:
  - 8 variable dropdowns.
  - 10 lightweight number controls.
  - 0 modal-scoped input number group add-ons.
  - no top global `可用变量` bar.
- Browser automation limitation:
  - textarea cursor movement by Home/Arrow and center click could not be reliably simulated, so arbitrary cursor insertion needs manual verification.
