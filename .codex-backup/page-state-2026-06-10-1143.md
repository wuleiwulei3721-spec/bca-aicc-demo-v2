# Page State - 2026-06-10 11:43 +08:00

## Formal Live Chat

- Formal `Live Chat` tab is still rendered by `LiveChat2Page`.
- Default Current sessions remain `livechat2-001` and `livechat2-005`, but they are treated as existing list customers and do not flash.
- Handoff sessions created through `requestLiveChatWorkspace()` continue to use generated ids such as `livechat2-001-handoff-1` and still flash briefly.
- Handoff sessions clone source messages and update message `timestamp` / `time` near the new access time.

## Conversation

- WhatsApp conversations hide agent message recall controls.
- Haloapps and Webchat conversations keep `Recall` and recalled-message `Re-edit`.
- Message Record opens in the right Assistant tab area and defaults its date range to the active session's latest message date.

## Customer Information

- PSTN and voice IVR interactions keep the existing final IVR `Menu` row and full IVR Journey modal.
- Haloapps text interactions display a single-level `Menu` using `lastMenuName ?? intent`.
- Haloapps channel detail shows `Haloapps Menu` with one TimelineFlow node.
- WhatsApp, Webchat, and Video do not receive an access menu hint from Live Chat.

## Styling

- Live Chat workspace tab unread badge is moved from `top: -8px` to `top: -4px`, with visible overflow on relevant tab elements.

## Verification State

- `npm run lint` passed.
- `npm run build` passed with the existing Vite chunk size warning.
- Browser smoke check passed for default no-flash, Message Record, WhatsApp recall hiding, Haloapps menu detail, PSTN IVR Journey, WhatsApp handoff flash, and unread badge bounds.
