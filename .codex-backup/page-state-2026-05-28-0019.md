# Page State - 2026-05-28 00:19 +08:00

## livechat2

- 菜单入口应位于 `Channel Simulation > WhatsApp` 下方。
- 点击后打开独立 `livechat2` tab。
- 客户列表保留收起/展开、渠道筛选、Current / History 左右切换和方形渠道图标。
- Conversation 保留快捷回复、消息记录、Transfer、End/Close、引用、撤回和发送演示能力。

## Modal Review Changes

- Transfer / Outbound Agent 查询栏保留 `Skill Queue` 和 `Status`。
- Agent 列表保留 `Skill Name` 和 `Status`，移除 `Department`。
- `Transfer Number` 保持号码输入框 + `Transfer` + `Conference` 单行布局。
- `Call Number` 保持 `Phone Number` + 号码输入框 + `Call` 单行布局。
- Internal Chat composer 保持回退后的两列版本：无图标 textarea + 纯文本 `Send`。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过。
- 仍建议在当前浏览器中人工复查 livechat2 和弹框交互路径。
