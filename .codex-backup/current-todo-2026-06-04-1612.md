# Current TODO - 2026-06-04 16:12 +08:00

## P0

- Manually verify `Routing Config > Media Service Rule Plans` Add/Edit in a real browser: click inside a message textarea at different positions, choose `插入变量`, and confirm the variable inserts at the cursor or replaces the selected range.
- Continue confirming the temporary Chinese modal copy and field grouping before converting the modal back to English.

## P1

- Decide the next `Media Service Rule Plans` scope for Voice and Video:
  - Share access/queue/non-working-time parameters where the semantics are the same.
  - Rename agent no-reply SLA fields before reusing them for voice/video answer or response SLA.
  - Keep Webchat recall, text no-reply close, and text auto response as text-only rules.
- Continue the planned `Channels` simplification after the rule plan modal is accepted: Channels should primarily bind Channel + Media Type to a rule plan.
- Consider replacing remaining generic CRUD `InputNumber addonAfter` usage later if AntD deprecation warnings become distracting for demos.

## P2

- After Chinese confirmation, convert Media Service Rule Plans modal labels, validation, buttons, and default messages back to English.
- Add a small unit or interaction test harness for variable insertion if the project later introduces frontend tests.
- Keep the Production customer branch behavior in mind: `main` hides `Call Management` and `Routing Config`; local `codex/text-channel-config-settings` keeps them visible for development.
