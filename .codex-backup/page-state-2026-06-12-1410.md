# Page State - 2026-06-12 14:10 +08:00

## Verification Rule V2 Page

- Route remains `/call-management/verification-rule-v2`.
- Existing V1 `/call-management/verification-rules` remains unchanged.
- Rule modal basic config:
  - Channel multi-select from active Routing Config channels.
  - Service Scenario / Skill Queue single-select from active skill queues.
  - Customer Segment multi-select.
  - Correct Required read-only.
  - Max Wrong Limit switch; numeric Max Wrong only appears when the switch is enabled.
  - Failure Action and Status remain configurable.
- Question Configuration:
  - No groups are shown by default in a new rule.
  - `Add Group` adds Mandatory, Dynamic, Static, or Alternative blocks.
  - Each block can be deleted.
  - `Select Questions` opens a searchable checkbox picker.
  - Confirmed questions appear in the block with ordering controls and per-question delete.
  - Alternative still has no required correct count.
- Question Bank:
  - Maintains question names only.
  - Default question names now come from the customer Excel original non-Chinese content.

## Inbound Verify Popup

- Still reads V2 rules from the shared demo store.
- Max Wrong unlimited rules do not show a Wrong counter.
- Max Wrong `0` only fails after the first wrong answer, not at zero wrong answers.
- Demo Conditions include `mbl d` as a case type for Merchant Solution special verification.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with chunk-size warning.
- HTTP smoke: `/`, `/call-management/verification-rules`, `/call-management/verification-rule-v2`, `/design-system` returned 200 on port 5173.
- Visual screenshot verification remains pending because Codex in-app browser connection timed out.
