# Page State - 2026-05-28 19:00 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## livechat2 Time State

- livechat2 打开时，服务时长从 mock `customer.accessDuration` 起算。
- active 未回复计时仍只在 active 会话展示，并且显示值不超过服务时长。
- `livechat2-005` customer-ended mock 当前服务时长为 `02:53`。

## livechat2 Ended State

- customer-ended / timeout-ended 的完整结束原因只显示在对话系统消息中。
- Conversation header 不再重复显示完整 ended notice。
- Ended 会话继续使用灰态头像、隐藏输入区、右侧 `Close`。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，仍保留既有 chunk size warning。
- Browser `/`：可加载 Home tab。
- Browser `/design-system`：可加载 Design System。
- Browser `/`：`livechat2` 菜单文字存在于 DOM，但当前自动化判断入口不可见，细节仍需人工复查。
