# Page State - 2026-05-28 01:28 +08:00

## livechat2 Customer List

- Header 保留渠道筛选和收起按钮。
- 展开态 Current / History 位于同一行，样式改为 Assistant 风格下划线 tab。
- 排序入口位于 Current / History 行右侧，按钮无边框，使用简洁菜单图标。
- 客户卡片保持两行信息，不显示最新消息时间，未回复计时不显示时钟图标。
- warning / breach 客户在展开态和收起态均使用左侧色条提示。
- 收起态不再显示头像左下角 SLA 小圆点。
- 收起态星标位于渠道头像内部右下角，只读展示，不支持点击。
- 转接图标和星标控件在展开态仍位于右侧同一行。

## Verification

- `npm run lint`：通过。
- `npm run build`：通过，仍保留既有 Vite/Rolldown chunk size warning。
- Browser `/`：可加载并可 Sign In；受当前 in-app browser 中侧栏按钮无可点击 bounding box/后续 CDP timeout 影响，未完成 `livechat2` 菜单入口自动化点击验证。
- Browser `/design-system`：页面可加载，标题为 `BANK 1 AICC Demo`。
- 待在浏览器中人工验证 livechat2 入口、展开态和收起态。
