# Current TODO - 2026-06-15 15:48 +08:00

## P0

- Manually verify `Call Management > Priority List Management` in the browser:
  - Default mock shows the four customer example groups.
  - Batch Add tooltip shows phone, social media and Email/Webchat examples.
  - Duplicate preview appears below the form.
  - Partial channel duplicates save only the remaining non-duplicate channels.
  - Fully duplicated input keeps the modal open and shows `All selected identifiers already exist.`
  - Delete, Search and Reset still behave as before.

## P1

- When connecting a real back end, enforce uniqueness by `Channel + normalized Identifier + Match Rule` server-side.
- Confirm with the customer whether domain matching should support any future variants beyond the explicit `@domain.tld` pattern.
- Consider whether duplicate rows should display existing record metadata beyond record ID if the real data model needs audit details.

## P2

- Add automated UI coverage for management-page batch add and duplicate filtering flows.
- Revisit Vite code-splitting if the existing chunk size warning becomes a deployment concern.
