# Page State - 2026-05-23 18:12 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## BankApp Demo

- Entry: `Customer Simulator > BankApp`.
- Workspace tab: `BankApp Demo`, closable.
- Layout: left Customer BankApp phone simulator, right AICC Process rail.
- Controls: `Customer Type`, `Next Step`, `Reset` inside AICC Process.
- Agent handoff tabs can open in the background so the customer-side phone can continue to the final rating screen.

## Screenshot State

- Channel selection:
  - `channel-selection-sanitized.png`
  - The visible `Voice Call`, `Video Call`, and `Live Chat` labels are enlarged.
- Guest phone number:
  - `voice-phone-number-sanitized.png`
- Guest Live Chat personal information:
  - `text-login-sanitized.png`
- Business selection:
  - Voice: `voice-business-selection-sanitized.png`
  - Video: `video-business-selection-sanitized.png`
  - Live Chat: `livechat-business-selection-sanitized.png`
- Business confirmation:
  - Voice: `voice-business-confirm-sanitized.png`
  - Video: `video-business-confirm-sanitized.png`
  - Live Chat: `livechat-business-confirm-sanitized.png`
- Video connected:
  - `video-connected.png`
- Live Chat queue:
  - `livechat-queue.png` exact user-provided processed attachment.
- Live Chat conversation:
  - `livechat-chat.png` exact user-provided processed attachment.
- Service Closed:
  - `service-closed.png` exact user-provided satisfaction evaluation attachment.

## Interaction State

- Voice:
  - Registered Customer: `Voice Call -> Select Business -> Confirm Business -> Calling Agent -> Connected -> Service Closed`.
  - Guest: `Voice Call -> Input Phone Number -> Select Business -> Confirm Business -> Calling Agent -> Connected -> Service Closed`.
  - Next from `Connected` opens the agent PSTN / Voice Call tab in the background.
  - Agent Customer Information channel: `mobile BankApp`.
- Video:
  - Registered Customer: `Video Call -> Select Business -> Confirm Business -> Calling Agent -> Connected -> Service Closed`.
  - Guest: `Video Call -> Input Phone Number -> Select Business -> Confirm Business -> Calling Agent -> Connected -> Service Closed`.
  - Connected screen displays `video-connected.png`.
  - Next from `Connected` opens the agent Video Call tab in the background.
  - Agent Customer Information channel: `mobile BankApp`.
  - Standard Channel Simulation Video Call still displays `video-camera Video Call`.
- Live Chat:
  - Registered Customer: `Live Chat -> Select Business -> Confirm Business -> Connecting to Agent -> Chat Page -> Service Closed`.
  - Guest: `Live Chat -> Personal Information -> Select Business -> Confirm Business -> Connecting to Agent -> Chat Page -> Service Closed`.
  - `Connecting to Agent` displays `livechat-queue.png`.
  - `Chat Page` displays `livechat-chat.png`.
  - Next from `Chat Page` opens the Live Chat workspace in the background and focuses `live-chat-002` BankApp customer Sari Amelia.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with existing chunk size warning.
- Browser `/`: passed for Live Chat queue exact image, Live Chat chat exact image, three-channel Service Closed image, and background handoff keeping BankApp Demo visible.
- Browser `/design-system`: passed.

## Rollback

- Remove `serviceClosed` from `src/mock/bankapp.ts` and restore `BankAppDemoPage` generated Service Closed UI if reverting to component-generated rating.
- Restore `BankAppDemoPage` Live Chat queue/chat rendering to generated UI if direct screenshot usage is no longer desired.
- Remove the optional `activate` behavior or restore default active-tab switching if the demo should immediately jump to the agent workspace after handoff.
