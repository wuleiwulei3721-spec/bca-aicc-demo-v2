# Page State - 2026-06-16 19:20 +08:00

## Inbound Workspace

- `/` renders `BasicLayout` and `AgentWorkspace`.
- Active call toolbar shows:
  - `IVR 08123456789` or `BankID 00012345`
  - `Skill Credit card activation`
- Incoming, Talking, Hold, and Mute show the call context; Idle and ended calls hide it.
- `Customer Information` no longer displays the bottom `Menu` route hint.
- Verification V2 initial Skill Queue mapping still uses the internal route menu calculation.

## Routing Config

- `/routing-config` defaults to `/routing-config/channels`.
- `/routing-config/*` is accessible unless `VITE_ENABLE_ADMIN_MENUS=false`.
- Left navigation shows `Routing Config` by default.
- `Channels` table:
  - Phone `Accounts` action is disabled.
  - Other channel account actions remain available.
- `Channels > Business Config`:
  - Webchat Text shows `Webchat Message Recall Limit (sec)`.
  - Other text channels do not show that Webchat-only field.

## Validation State

- TypeScript, lint, and build passed.
- HTTP smoke returned 200 for `/`, `/routing-config/channels`, `/routing-config/skill-routing-rules`, `/routing-config/business-types`, and `/design-system`.
- Browser screenshot/DOM verification remains pending because the in-app browser connection timed out.
