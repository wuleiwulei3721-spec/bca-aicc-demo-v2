# Page State - 2026-05-21 11:43 +08:00

## 路由

- `/`：主工作台，`BasicLayout` 包裹 `AgentWorkspace`。
- `/design-system`：设计系统展示页，`BasicLayout` 包裹 `DesignSystem`。
- `*`：重定向到 `/`。

## Surface Layer System

- L0 App Background：`--aicc-surface-l0`，应用底层蓝灰背景。
- L1 Workspace Surface：`--aicc-surface-l1`，`aicc-content` 主工作区。
- L2 Card Surface：`--aicc-surface-l2`，卡片、CRM 面板、Assistant 面板。
- L3 Modal Surface：`--aicc-surface-l3`，所有 `BaseModal` 浮层。
- L4 Header / Active Surface：`--aicc-surface-l4`，Header、Active tab、selected controls。

## 页面状态

- Header 显示 `BANK 1`，不再使用旧品牌或重蓝色块。
- Browser Title 为 `BANK 1 AICC Demo`。
- Home tab 保持工作台待机态。
- Ready 后自动打开 Inbound tab。
- CRM 区域使用代码内 BANK 1 CRM fallback。
- Assistant 区域使用代码内 BANK 1 Assistant fallback。
- `/design-system` 已展示 L0-L4 surface tokens。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，保留 Vite chunk size warning。
- 浏览器已检查 `/`、`/design-system`、签入后 Inbound DOM。
- Modal 需要后续继续做可视检查。
