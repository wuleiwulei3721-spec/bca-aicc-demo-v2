# Current TODO - 2026-06-02 21:47 +08:00

## P0

- Manually review `Routing Config > Sites`:
  - no top timezone hint;
  - no `Country` / `Country Code` in table or modal;
  - search conditions are `Keyword + Status`;
  - Keyword placeholder is `Site ID / Name`;
  - Add modal title is `Add Site`.

- Manually review `Routing Config > VDN`:
  - search conditions are `Keyword + Status`;
  - Keyword placeholder is `VDN ID / Name / Platform ID`;
  - Keyword fuzzy matching feels clear to users.

## P1

- Decide whether other master-data pages should also adopt `Keyword + Status` instead of a single generic Keyword search.
- If Sites truly should not carry country in the data contract, remove `countryCode` from types and mock in a later scoped change.

## P2

- Consider adding a small tooltip or placeholder convention for Keyword filters to clarify which fields are searched.

