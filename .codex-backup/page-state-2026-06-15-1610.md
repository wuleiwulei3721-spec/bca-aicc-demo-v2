# Page State - 2026-06-15 16:10 +08:00

## Priority List Management

Route: `/call-management/priority-list`

Current duplicate preview behavior:

- Duplicate detection uses the full front-end demo store.
- Duplicate rows show:
  - `Channel`
  - `Identifier`
  - `Match Rule`
  - `Existing No.`
- `Existing No.` is the visible table-style sequence number, not the internal id.
- Example: the default email/webchat record `PL009` is displayed as `9`.

Unchanged behavior:

- Duplicate definition remains `Channel + normalized Identifier + Match Rule`.
- Repeated channels are skipped automatically.
- Non-duplicate channels are still saved.
- Fully duplicate input keeps the modal open.

## Verification

- TypeScript, lint and production build passed.
- `/call-management/priority-list` HTTP smoke passed.
- Browser visual validation is pending.
