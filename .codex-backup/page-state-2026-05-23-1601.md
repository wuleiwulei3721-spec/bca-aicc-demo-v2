# Page State - 2026-05-23 16:01 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## BankApp Demo

- Entry: `Customer Simulator > BankApp`.
- Workspace tab: `BankApp Demo`, closable.
- Layout: left Customer BankApp phone simulator, right AICC Process rail.
- Controls: `Customer Type`, `Next Step`, `Reset` inside AICC Process.

## Screenshot State

- Channel selection:
  - `channel-selection-sanitized.png`
- Guest phone number:
  - `voice-phone-number-sanitized.png`
- Livechat personal information:
  - `text-login-sanitized.png`
- Business selection:
  - Voice: `voice-business-selection-sanitized.png`
  - Video: `video-business-selection-sanitized.png`
  - Livechat: `livechat-business-selection-sanitized.png`
- Business confirmation:
  - Voice: `voice-business-confirm-sanitized.png`
  - Video: `video-business-confirm-sanitized.png`
  - Livechat: `livechat-business-confirm-sanitized.png`

## Interaction State

- `Select Business` is now screenshot-driven and has transparent card hotspots.
- Clicking a business hotspot selects a business type and opens `Confirm Business`.
- `Confirm Business` is now screenshot-driven and has transparent No / Yes hotspots.
- No returns to `Select Business`; Yes proceeds to `Calling Agent`.
- `Calling Agent` and later pages remain generated UI.
- Voice, Video, and Livechat still use the same final handoff behavior:
  - Voice -> PSTN / Voice Call with `BankApp Voice`
  - Video -> Video Call with `BankApp Video`
  - Livechat -> Live Chat focused on BankApp customer Sari Amelia

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with existing chunk size warning.
- Browser `/`: passed for Voice, Video, and Livechat business selection and confirmation image paths.
- Browser `/`: Voice confirmation Yes hotspot proceeds to `Calling...`.
- Browser `/design-system`: passed.

## Rollback

- Restore component-based `renderBusinessScreen` and `renderConfirmScreen` in `src/pages/bankapp/BankAppDemoPage.tsx`.
- Remove `businessSelection` and `businessConfirm` screenshot maps from `src/mock/bankapp.ts`.
- Remove `.bankapp-business-hotspot*` and `.bankapp-confirm-hotspot*` styles if no longer needed.
