# Page State - 2026-05-28 18:00 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> redirect to `/`

## livechat2 History List

- History 展开态客户行第一行右侧显示结束时间。
- 结束时间来源优先级：
  - 真实 ended 会话：`liveChat2SessionStatuses[sessionId].endedAt`。
  - 初始 mock 历史会话：`lastMessageAt`。
- 时间格式：
  - 当天：`Ended HH:mm:ss`。
  - 非当天：`Ended MM-DD HH:mm:ss`。
- 当前列表不显示结束时间。
- 面板收起态不显示结束时间。

## 验证状态

- `npm run lint`：通过。
- `npm run build`：通过，保留既有 chunk size warning。
- Browser `/`：可加载 Home tab。
- Browser `/design-system`：可加载 Design System。
- livechat2 菜单点击与 History 视觉仍需人工复查。
