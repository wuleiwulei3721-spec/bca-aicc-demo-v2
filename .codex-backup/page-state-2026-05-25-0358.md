# Page State - 2026-05-25 03:58 +08:00

## 路由

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `*` -> 重定向到 `/`

## Live Chat 视觉状态

- Live Chat tab 无 active session 时不显示 duration、不闪烁。
- Live Chat 新 active session 进入时，label 带 `workspace-tab-label--tab-flash`，整个 `.ant-tabs-tab` 执行 `aicc-interaction-tab-flash`。
- PSTN / Voice / Video 交互 tab 仍使用 label 级 `workspace-tab-label--flash`，不受 Live Chat tab 级 flash 影响。
- 客户列表新接入 flash 使用整行 overlay：`::after { inset: 0; border-radius: inherit; }`。
- Live Chat SLA warning / breach 统一使用 token：
  - warning：`#f59e0b`
  - breach：`#f04438`
- tab duration、客户列表 duration、Conversation header timer、SLA marker 与左侧 accent 使用同一套颜色。

## 验证记录

- `npm run lint`：通过。
- `npm run build`：通过。
- Browser `/`：WhatsApp / BankApp Live Chat 新接入 flash 范围与颜色符合预期。
- Browser `/design-system`：正常加载。

## 已知限制

- Live Chat tab 整块 flash 依赖 CSS `:has()`。
- 本轮没有新增真实消息到达事件；仅优化现有新 active session 提示。
