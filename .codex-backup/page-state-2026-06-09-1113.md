# Page State - 2026-06-09 11:13 +08:00

## Routes

- `/login` is public and renders the BANK 1 login page.
- Authenticated visits to `/login` redirect to `/`.
- Unauthenticated visits to `/`, `/design-system`, and `/call-management/*` redirect to `/login`.
- `/` loads `BasicLayout -> AgentWorkspace` after auth.
- `/call-management/verification-rules`, `/call-management/text-channel-settings`, and `/call-management/busy-reasons` remain protected and customer-visible after auth.
- `/routing-config/*` continues to redirect to `/`.

## Login

- Fields: User Name, Password, EXT, PIN/captcha.
- Demo success: `admin / 888888` plus current PIN.
- Demo failure: wrong PIN shows a local PIN error and refreshes PIN; wrong credentials show the mock LDAP error and refresh PIN.
- Successful login enters `/` with agent state still `Unsigned`.

## Agent Sign-In

- Unsigned profile dropdown shows `Sign in - Voice only`, `Sign in - Digital only`, `Sign in - Voice + Digital`, and `Log Out`.
- Signed-in profile line shows `PBK BSB · {mode}`.
- Signed-in dropdown shows current mode, AUX group, enabled busy reasons, `Sign Out`, and `Log Out`.
- `Sign Out` clears media sign-in and interaction state only.
- `Log Out` clears auth session and returns to `/login`.

## Skill Rules

- `Voice only`: voice/video flows are allowed; live chat handoff is blocked.
- `Digital only`: live chat is allowed; PSTN, BankApp Voice, and BankApp Video are blocked.
- `Voice + Digital`: existing PSTN, BankApp, WhatsApp, Live Chat, Call Management, and Design System flows are allowed.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with existing chunk size warning.
- Headless Chrome checked login route guard, login failures, login success, sign-in options, `Digital only` PSTN block, `Voice + Digital` PSTN allow, and `/design-system`.
