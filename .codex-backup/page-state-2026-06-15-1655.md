# Page State - 2026-06-15 16:55 +08:00

## Routes

- `/`: HTTP 200.
- `/call-management/verification-rule-v2`: HTTP 200.
- `/design-system`: HTTP 200.

## Verification Rule V2

- Outer list remains standard AdminPage/AdminTable style.
- Edit/Add modal:
  - Basic Config uses compact aligned controls.
  - Overall rule visible fields remain `Channel`, `Skill Queue`, `Customer Segment`, `Status`.
  - Scenario strip is compact and name-only.
  - Default scenario can be renamed, cannot be deleted.
  - Scenario Policy shows:
    - `Correct Required` read-only.
    - `Max Wrong` with `No Limit` switch.
    - `Action` select only when wrong limit is enabled.
    - `Agent Hint`.
  - Outer list no longer shows a standalone `Failure Action` column.
  - Question block configuration remains unchanged.
- Question Bank:
  - Search and add/edit remain unchanged.
  - Delete checks whether the question is referenced by V2 scenario blocks.
  - Referenced questions require confirmation before deletion.

## Customer Verification Popup

- Still reads scenario-level `Max Wrong`, `Failure Action`, and `Agent Hint`.
- No strict order enforcement exists yet.
- Wrong count remains hidden when current scenario has `No Limit`.

## Validation State

- TypeScript, lint, build, sensitive scan, and HTTP smoke checks passed.
