# Page State - 2026-06-02 17:58 +08:00

## Routes

- `/` -> `BasicLayout` -> `AgentWorkspace`
- `/design-system` -> `BasicLayout` -> `DesignSystem`
- `/call-management/routing-configuration` -> `BasicLayout` -> `RoutingConfigurationPage`
- `/call-management/text-channel-settings` -> `BasicLayout` -> `TextChannelSettingsPage`
- `*` -> redirect to `/`

## Routing Configuration

- Menu path: `Call Management > Routing Configuration`.
- Header actions: `Save Draft`, `Publish Preview`; both are demo-only display actions.
- Overview cards show enabled route factors, active routing rules, skill queues, and pre-publish risks.
- Pre-publish validation checks:
  - site ratio total equals 100%
  - business type code is two digits
  - all demo IDs are explicit uppercase codes
  - active rules target active skill queues
  - target queues have active work time plans
  - assigned-agent coverage warning
- Tabs:
  - `Route Elements`: route factor table, enable switches, enabled factor model.
  - `VDN`: VDN/access point table.
  - `Sites`: access site table.
  - `Channels & Media`: channel table plus `channel_media` table with concurrency, scan mode, scan interval, and extension config.
  - `Access Accounts`: channel-specific account table with `secret_ref` instead of secrets.
  - `Access Entries`: DNIS/app/mailbox/entry mapping table.
  - `Business Types`: two-digit business type codes plus media/language dictionaries.
  - `Site Access Volume`: channel + media site ratio groups.
  - `Working Time Plans`: default 24x7 and custom bank-hour plans.
  - `Skill Queues`: reusable queue targets, work time plan, max queue size, timeout, prompts, read-only agent count.
  - `Skill Routing Rules`: batch factor selection, duplicate detection, overwrite option, active rule table, published routing index.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with existing chunk size warning.
- Local Vite server: `http://127.0.0.1:5174/`; 5173 was already occupied.
- Chrome headless smoke check:
  - `/` loaded BANK 1 shell.
  - `/design-system` loaded BANK 1 shell and Design System content.
  - `/call-management/routing-configuration` loaded BANK 1 shell and rendered `Route Elements` / `Skill Routing Rules`.
  - `/call-management/text-channel-settings` still rendered `Text Channel Settings` / `Service Rules` / `Channel Queue Alerts`.
