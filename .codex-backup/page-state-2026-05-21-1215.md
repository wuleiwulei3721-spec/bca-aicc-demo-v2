# Page State - 2026-05-21 12:15 +08:00

## 路由

- `/`：主工作台，`BasicLayout` 包裹 `AgentWorkspace`。
- `/design-system`：设计系统展示页，`BasicLayout` 包裹 `DesignSystem`。
- `*`：重定向到 `/`。

## 页面状态

- Header 显示 `BANK 1`，并保持旧版蓝色渐变顶部栏。
- CRM 固定 tab 优先显示 `public/screenshots/crm-workspace.jpg`。
- Assistant tab 优先显示 `public/screenshots/assistant-workspace.jpg`。
- CRM/Assistant 截图加载失败时显示代码内 BANK 1 fallback。
- Connection tab 仍显示代码内连接状态面板。
- Modal/Dialog 本轮未继续调整。

## 验证状态

- CRM 图片 URL：HTTP 200。
- Assistant 图片 URL：HTTP 200。
- `npm run lint`：通过。
- `npm run build`：通过，保留既有 Vite chunk size warning。
- 浏览器截图级验证：当前 Codex in-app browser pane 不可用，后续需要补做。
