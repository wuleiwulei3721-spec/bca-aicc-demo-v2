# Current TODO - 2026-06-12 17:51 +08:00

## P0

- Completed: Browser check `/design-system` Admin Management Page example.
- Completed: Browser check `/call-management/verification-rule-v2`:
  - Filter toolbar stays one row when space allows.
  - Question Bank / Add stay on the right.
  - Main table defaults to 20 rows and Actions stays fixed right.
  - Question Bank modal uses internal table scroll.
- Completed: Browser check `/call-management/blacklist`, `/call-management/priority-list`, `/call-management/busy-reasons`.
- Completed: Browser check `/routing-config/business-types` and `/routing-config/skill-routing-rules`.

## P1

- If visual review finds old field styles inside complex Routing Config modals, migrate those inner fields to `AdminFormField`.
- Consider migrating `Global Control Configuration` to `AdminPage` for page scroll consistency, while keeping its form-centric layout.
- Consider adding a lightweight automated smoke test for AdminTable pagination and Actions fixed column.

## P2

- Add a short docs page or README section with admin page implementation examples.
- Audit any future Call Management page before release to ensure it imports from `src/components/admin/*`.
