# Page State - 2026-06-13 17:23 +08:00

## Verification Rule V2 Admin

- `Special Rules` supports `Add Special Scenario`.
- A special scenario block includes `Scenario Name`, `Questions N`, `Select`, `Correct`, `Order`, and delete.
- Special scenario questions reuse the existing question picker, search, checkbox selection, ordering, and per-question delete controls.
- `Organization Segment Override` remains a separate optional block with fixed `O1-O3` and `O4-O5` fields.

## Customer Verification V2 Popup

- `Verification Scenario` contains `Special Scenario` and `Organization Segment`.
- `Special Scenario` defaults to `None`.
- Options are read from the matched V2 rule's configured special scenarios.
- Selecting a special scenario appends its questions as the special verification group.
- Scenario question IDs include the scenario ID, so changing scenarios removes old special answers while base answers remain.

## Validation State

- Lint passed.
- Build passed.
- Sensitive-word scan under `src/` passed.
- HTTP smoke checks passed for the main workspace, V2 admin page, and design system.
