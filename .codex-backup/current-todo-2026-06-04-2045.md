# Current TODO - 2026-06-04 20:45 +08:00

## P0

- Manually verify `/routing-config/channel-types`: English UI, 13 channel types, Phone shows no access parameters, Email shows IMAP/SMTP template fields, no Add/Delete actions.
- Manually verify `/routing-config/channels`: no Add/Delete Channel, Channel Type column is present, actions are Edit / Business Config / Accounts.
- Manually verify Channel Edit: Media Type and Status are editable; Channel ID, Channel Name, Channel Type are read-only; technical access parameters follow the channel type template.
- Manually verify Business Config: Text shows full text-only configuration without queue settings; Voice and Video show access configuration only.
- Manually verify Account Management: Instagram shows multiple accounts under one channel; Email Contact and Email Priority are separate Email channels with different mail server settings.
- Manually verify `/routing-config/access-accounts` and `/routing-config/media-service-rule-plans` redirect to `/routing-config/channels` in a real browser.
- Manually verify `/routing-config/skill-queues`: queue fields display and save correctly.

## P1

- Decide whether to fully delete the unreachable `MediaServiceRulePlansPage` source after the Channels business configuration is accepted.
- Decide whether `Channel Type` should display license metadata beyond `Licensed / Unlicensed` in a later backend-connected version.
- Consider adding lightweight automated browser smoke tests once Playwright or browser automation is available.

## P2

- Consider replacing remaining generic CRUD `InputNumber addonAfter` usage later if AntD deprecation warnings become distracting for demos.
- Consider backend API shape for Channel Type templates, Channel access parameters, and Channel Account credential references.
