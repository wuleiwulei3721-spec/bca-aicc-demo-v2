# Page State - 2026-06-11 20:52 +08:00

## Blacklist Management

- Route: `/call-management/blacklist`.
- Page title: `Blacklist Management`.

## Channel Query Source

- Search Channel dropdown:
  - `All`
  - Active `Routing Config > Channels` names from `routingConfigStore.channels`.
- The search dropdown no longer uses a hardcoded blacklist-specific channel enum.

## Add / Batch Add Channel Source

- Add modal Channel dropdown:
  - `All Channels`
  - Active `Routing Config > Channels` names.
- Batch Add modal Channel dropdown:
  - Same as Add modal.

## Verification

- `npm.cmd run lint`: passed.
- `npm.cmd run build`: passed with existing Vite/Rolldown chunk warning.
