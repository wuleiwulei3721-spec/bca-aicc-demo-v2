# Page State - 2026-06-06 14:55 +08:00

## `/`

- `BasicLayout`、Agent status、话务工具条、Channel Simulation、Live Chat 固定 tab 逻辑保持既有状态。
- PSTN 入口仍会创建 `PSTN` call tab；Customer Information 初始可显示未识别客户、身份刷新、编辑联系人、`Verify` 和 `Menu`。
- `Verify` 现在打开 Customer Verification Assist，而不是固定 10 问列表。

## Customer Verification Assist

- 顶部显示 `Verification Channel Type` 和 `Verification Business Type`。
- 业务类型选项来自启用 Business Types mock：`Perbankan`、`Kartu Kredit`、`Paylater`。
- 规则组合当前覆盖：
  - `Phone + Perbankan`
  - `HaloApp Registered + Perbankan`
  - `Phone + Kartu Kredit`
  - `HaloApp Registered + Kartu Kredit`
  - `HaloApp Registered + Paylater`
- 问题分组支持 `Mandatory`、`Dynamic`、`Static`、`Alternative`、`Layering`、`Special`。
- 弹窗展示 demo answer 和 answer source；这是演示便利，不代表生产可见口径。
- 达标后 `Apply Verified` 可用；错答达到上限后 `Apply Failed` 可用。

## BankApp / HaloApp PIN

- BankApp Demo 仍显示脱敏名 `BANK App`。
- 坐席在 BankApp Voice 验证弹窗中点击 `Send PIN Verification` 后，BankApp Demo 手机内容切换到 4 位 PIN 输入页。
- 客户点击 `Submit PIN` 后，process rail 显示 `Customer PIN verified in BANK App.`。
- PIN verified 后，坐席重新打开验证弹窗会看到 `HaloApp Registered` 渠道类型，并加载 registered 规则。

## `/design-system`

- 本轮未改 Design System 页面结构。
- Browser smoke check 已确认页面正常加载。

## 验证命令

- `npm run lint`：通过。
- `npm run build`：通过，仍只有既有 Vite chunk size warning。
