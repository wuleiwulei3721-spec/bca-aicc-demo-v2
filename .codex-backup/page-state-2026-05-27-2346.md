# Page State - 2026-05-27 23:46 +08:00

## Internal Chat Modal

- 底部 composer 当前为上上版本：
  - `aicc-internal-chat__composer-box` 两列布局。
  - 左侧为无边框 textarea。
  - 右侧为纯文本 `Send` 按钮。
  - 无 Emoji 按钮。
  - 无 Upload image / Attach file 按钮。
  - 无 Send icon。

## 验证状态

- `npm run lint`：重新执行通过。
- `npm run build`：通过。
- Browser DOM 检查：Internal Chat 弹框中无 `Emoji` / `Choose emoji` / `Upload image` / `Attach file` / `send` icon，存在 textarea 和纯文本 `Send` button。
