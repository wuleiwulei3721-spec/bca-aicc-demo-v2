# Current TODO - 2026-06-15 16:10 +08:00

## P0

- Manually verify Priority List duplicate preview:
  - Existing row for default record 9 displays `Existing No.` as `9`, not `PL009`.
  - Partial duplicate channel filtering still saves remaining non-duplicate channels.
  - Fully duplicated input still keeps the modal open and shows `All selected identifiers already exist.`

## P1

- If the customer expects `Existing No.` to follow the currently filtered table sequence instead of the full list, clarify that behavior before changing it.

## P2

- Add automated UI coverage for duplicate preview display values.
