# Page State - 2026-06-13 18:52 +08:00

## Verification Rule V2 Admin

- Special scenario rows include a `Mode` select with `Append` and `Replace`.
- New special scenarios default to `Append`.
- Existing scenarios without `mode` are treated as `Append` when cloned/read.
- `Max Wrong` remains a rule-level field and is not duplicated inside special scenarios.

## Customer Verification Popup

- `None` uses the base rule.
- `Append` scenarios add special questions after base questions.
- `Replace` scenarios hide base questions and show only the selected special scenario questions.
- The popup still allows only one selected special scenario at a time.

## Default Data

- `ATO / add-on` is append.
- `Khusus laporan mbl d` is replace.
- `Branch Combined Verification` is replace and remains fixed at six required correct answers.

## Validation State

- Lint passed.
- Build passed with the existing chunk size warning.
- Sensitive-word scan under `src/` passed.
- HTTP checks for `/`, `/call-management/verification-rule-v2`, and `/design-system` returned 200.
- Diff whitespace check passed with Windows line-ending warnings only.
