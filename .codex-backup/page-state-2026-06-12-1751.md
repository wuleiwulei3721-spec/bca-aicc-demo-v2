# Page State - 2026-06-12 17:51 +08:00

## Design System

- Route: `/design-system`
- Added `Admin Management Page` section.
- Shows standard maintenance query toolbar, right-side primary actions, AdminTable pagination, contract tags, and AdminModal footer.

## Call Management

- `Verification Rules`
  - Uses AdminPage, AdminTable, AdminModal, AdminModalFooter.
  - Business behavior remains unchanged.
- `Verification Rule V2`
  - Uses AdminPage, AdminToolbar, AdminFilterField, AdminTable, AdminModal, AdminModalFooter.
  - Main list uses standard 20-row pagination.
  - Question Bank modal uses modal AdminTable with internal vertical scroll.
  - Question Bank/Add stay in the right-side toolbar action area.
- `Blacklist Management`
  - Uses AdminPage, AdminToolbar, AdminFilterField, AdminTable, AdminModal, AdminModalFooter.
  - Add / Batch Add / Delete are grouped on the right side of the query toolbar.
- `Priority List Management`
  - Same admin structure as Blacklist Management.
- `Busy Reason Management`
  - Uses AdminPage, AdminToolbar, AdminFilterField, AdminTable, AdminModal, AdminModalFooter.

## Routing Config

- Ordinary CRUD pages through `RoutingConfigCrudPage` now use Admin components.
- Custom pages in `RoutingConfigDataPages.tsx` now use AdminPage/AdminTable/AdminModal.
- `Skill Routing Rules` now uses AdminPage/AdminTable/AdminModal.
- Main lists inherit standard pagination text, page size options, and body font weight.

## Validation State

- `npm run lint`: passed.
- `npm run build`: passed with existing chunk size warning.
- Headless Chrome CDP preview checks: passed for `/design-system`, `/call-management/verification-rule-v2`, `/call-management/blacklist`, `/call-management/priority-list`, `/call-management/busy-reasons`, `/routing-config/business-types`, and `/routing-config/skill-routing-rules`.
