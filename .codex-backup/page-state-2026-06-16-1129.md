# Page State - 2026-06-16 11:29 +08:00

## Routes

- `/call-management/priority-list`: HTTP 200.
- `/call-management/blacklist`: HTTP 200.

## Priority List Management

- Query:
  - `Channel` is a multi-select; empty value means all channels.
  - `Identifier` is a text keyword filter.
- Toolbar:
  - right-side actions are `Batch Add` and `Delete`.
  - there is no separate single-record `Add` button.
- Batch Add:
  - Channel is multi-select and uses enabled routing channels.
  - Identifier is an 8-row textarea with semicolon-separated batch input.
  - Save expands every selected channel and every unique identifier into a separate list record.
- List:
  - columns include `No.`, `Channel`, `Identifier`, `Match Rule`, `Remark`, `Created Date`, and `Created By`.
  - each row has exactly one Channel.
  - checkbox selection and bulk delete remain enabled.
- Match Rule:
  - `Webchat`, `Email Contact`, or `Email Priority` plus a valid `@domain.tld` style identifier displays `Email Domain Match`.
  - all other values and channels display `Exact Match`.
- Duplicate handling:
  - duplicate key is `Channel + normalized Identifier + Match Rule`.
  - duplicates are previewed and skipped automatically.

## Validation State

- TypeScript, lint, production build, HTTP smoke, and `git diff --check` passed.
- Build still reports the existing large chunk warning.
- Screenshot-level browser validation remains pending because the Browser tool was not available in this session.
